// <auto-generated>
//     This code was generated by CSharpBinderGeneratorService.
//     Do not modify this file directly.
// </auto-generated>

#nullable enable

using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using Cysharp.Threading.Tasks;

using Gs2.Unity.Core;
using Gs2.Unity.Util;
using GS2Studio.Generated.MissionCollection;
using GS2Studio.Generated.MissionCounter;
using Gs2.Unity.Core.Model;
using Gs2.Unity.Gs2Mission.Model;
using Gs2Bind.Gs2Mission;

namespace GS2Studio.Generated.Mission
{
    /// <summary>
    /// Pure read contract for <see cref="MissionBinder"/>: the read-only
    /// model surface (<see cref="Mission"/>) plus the single-fetch
    /// read navigation. Substitutable seam — code holding a binder for reads
    /// can be faked against this interface. No server-side operations and no
    /// lifecycle (Subscribe/Dispose/Mount).
    /// </summary>
    public interface IReadOnlyMissionBinder : Mission
    {
    }

    /// <summary>
    /// Actionable contract for <see cref="MissionBinder"/>: the read contract
    /// plus the delegated-action dispatchers (server-side operations). This is
    /// the element type yielded by the companion Collection (enumeration /
    /// <c>Get</c> / <c>Where*</c>), so actions stay callable from collection
    /// results. Still no lifecycle (Subscribe/Dispose/Mount).
    /// </summary>
    public interface IActionableMissionBinder : IReadOnlyMissionBinder
    {
        Task Receive(Gs2.Unity.Gs2Mission.Model.EzConfig[]? config = null);
    }

    /// <summary>
    /// Owning contract for <see cref="MissionBinder"/>: adds the lifecycle
    /// surface (Subscribe / Invalidate / Dispose) over <see cref="IActionableMissionBinder"/>.
    /// Returned by the binder factory; a fake implementation substitutes the
    /// whole binder.
    /// </summary>
    public interface IMissionBinder : IActionableMissionBinder, IDisposable
    {
        void Subscribe(Action? onChange = null);
        void Invalidate();
    }

    /// <summary>
    /// Lifecycle-free base for <see cref="MissionBinder"/>.
    /// Owns <c>_model</c> / <c>_gs2</c> / <c>_session</c> and implements the
    /// read-only <see cref="Mission"/> contract by forwarding each
    /// model property to <c>_model</c> (so consumers read <c>binder.{Prop}</c>
    /// directly). This lets binders reached through a non-owning view
    /// (collection <c>Get</c> / <c>Where*</c> / enumeration) still read their
    /// model. Never exposes <c>Dispose</c>, <c>Subscribe</c>, or
    /// <c>MountAsync</c>; the disposed-guard hook is a virtual no-op overridden
    /// by the owning derived class. Delegated actions live on the owning
    /// derived class (every binder instance is the owning type).
    /// </summary>
    public class ReadOnlyMissionBinder : IReadOnlyMissionBinder
    {
        // `private protected` because the mutable model type is internal; only
        // the same-assembly owning derived binder reads/writes this field.
        private protected readonly MutableMission _model;
        protected readonly Gs2Domain _gs2;
        protected readonly IGameSession _session;

        /// <summary>
        /// Internal mutable view of the bound model. Reconcile paths on the
        /// owning collection write through this; external consumers only see
        /// the read-only <c>Mission</c> surface this binder implements.
        /// </summary>
        internal MutableMission MutableModel => _model;
        /// <inheritdoc cref="Mission.Id" />
        public MissionId Id => _model.Id;
        /// <inheritdoc cref="Mission.MissionCollection" />
        public MissionCollectionId MissionCollection => _model.MissionCollection;
        /// <inheritdoc cref="Mission.Counter" />
        public MissionCounterId Counter => _model.Counter;
        /// <inheritdoc cref="Mission.TargetValue" />
        public long TargetValue => _model.TargetValue;
        /// <inheritdoc cref="Mission.Completed" />
        public bool Completed => _model.Completed;
        /// <inheritdoc cref="Mission.Received" />
        public bool Received => _model.Received;
        /// <inheritdoc cref="Mission.RewardAmount" />
        public int RewardAmount => _model.RewardAmount;
        /// <summary>
        /// Base constructor. Stores the bound model + service handles on the
        /// protected fields shared with the owning derived class. `private
        /// protected` because the mutable model parameter is internal (only the
        /// same-assembly owning binder constructs through here).
        /// </summary>
        private protected ReadOnlyMissionBinder(MutableMission model, Gs2Domain gs2, IGameSession session)
        {
            _model = model ?? throw new ArgumentNullException(nameof(model));
            _gs2 = gs2 ?? throw new ArgumentNullException(nameof(gs2));
            _session = session ?? throw new ArgumentNullException(nameof(session));
        }

        /// <summary>
        /// Disposed-guard hook for navigation declared on this read base. The
        /// single-fetch <c>Get{Child}</c> sugar lives here (so it is callable
        /// through the read contract <see cref="IReadOnlyMissionBinder"/>) and
        /// calls this hook. The base is a no-op; the owning derived class
        /// overrides it to route to <c>ThrowIfDisposed</c>, so a disposed binder
        /// still throws (every binder instance is the owning derived type).
        /// Parent-side <c>Get{Child}s</c> sugar stays on the owning class — it
        /// owns the lazily-built child Collection root it disposes.
        /// </summary>
        protected virtual void ThrowIfDisposedForNavigation() { }
    }

    /// <summary>
    /// Binder for Mission
    /// Binds GS2 resource values to the model and automatically reflects server-side changes
    /// </summary>
    public sealed class MissionBinder : ReadOnlyMissionBinder, IMissionBinder
    {
        private readonly List<Action> _unsubscribers = new List<Action>();
        private bool _disposed;
        internal bool _mounted;

        private readonly MissionTaskModelAcquireActionArrayLoader __transactionAcquireActionLoader;
        private readonly MissionTaskModelLoader __missionMissionMissionTaskModelLoader;
        private readonly CompleteLoader __membershipMissionCompleteLoader;

        /// <summary>
        /// Internal constructor. External construction must go through <c>CreateAsync</c>
        /// or one of the list static factories on the companion Collection class.
        /// </summary>
        internal MissionBinder(
            MutableMission model,
            Gs2Domain gs2,
            IGameSession session
        ) : base(model, gs2, session)
        {
            __transactionAcquireActionLoader = new MissionTaskModelAcquireActionArrayLoader("Mission", _model.MissionCollection, _model.Id);
            __missionMissionMissionTaskModelLoader = new MissionTaskModelLoader("Mission", _model.MissionCollection, _model.Id);
            __membershipMissionCompleteLoader = new CompleteLoader("Mission", _model.MissionCollection);
        }

        /// <summary>
        /// Creates a Mission instance from identity parameters. The
        /// returned model carries the identity state required by loaders
        /// (`_model.Id` and any `_model.{Prop}` references in loader constructors).
        /// </summary>
        internal static MutableMission CreateModel(MissionId id, MissionCollectionId missionCollection)
        {
            var model = new MutableMission();
            model.Id = id;
            model.MissionCollection = missionCollection;
            return model;
        }

        /// <summary>
        /// Creates a MissionBinder and mounts it to GS2 resources.
        /// This is the only external construction path for mount-based usage.
        /// </summary>
        public static async Task<MissionBinder> CreateAsync(
            MissionId id, MissionCollectionId missionCollection,
            Gs2Domain gs2,
            IGameSession session,
            CancellationToken cancellationToken = default)
        {
            var model = CreateModel(id, missionCollection);
            var binder = new MissionBinder(model, gs2, session);
            await binder.MountAsync(cancellationToken);
            return binder;
        }

        /// <summary>
        /// Loads GS2 resource values and mounts them to the model
        /// </summary>
        /// <param name="cancellationToken">Cancellation token</param>
        public async Task MountAsync(CancellationToken cancellationToken = default)
        {
            ThrowIfDisposed();
            cancellationToken.ThrowIfCancellationRequested();
            var _transactionAcquireAction = await __transactionAcquireActionLoader.Load(_gs2, _session);
            cancellationToken.ThrowIfCancellationRequested();
            if (_transactionAcquireAction != null)
            {
            }
            var _missionMissionMissionTaskModel = await __missionMissionMissionTaskModelLoader.Load(_gs2, _session);
            cancellationToken.ThrowIfCancellationRequested();
            ApplyMissionMissionMissionTaskModel(_model, _missionMissionMissionTaskModel);
            var _membershipMissionComplete = await __membershipMissionCompleteLoader.LoadOrNull(_gs2, _session);
            cancellationToken.ThrowIfCancellationRequested();
            ApplyMembershipMissionComplete(_model, _membershipMissionComplete);
            _mounted = true;
        }

        /// <summary>
        /// Subscribes to GS2 resource changes and automatically updates the model
        /// </summary>
        /// <param name="onChange">Callback on change (optional)</param>
        public void Subscribe(Action? onChange = null)
        {
            ThrowIfDisposed();
            _unsubscribers.Add(__transactionAcquireActionLoader.Subscribe(
                _gs2,
                _session,
                (_, _, value) =>
                {
                    if (_disposed) return Task.CompletedTask;
                    if (value != null)
                    {
                    }
                    return Task.CompletedTask;
                },
                () => onChange?.Invoke()
            ));
            _unsubscribers.Add(__missionMissionMissionTaskModelLoader.Subscribe(
                _gs2,
                _session,
                (_, _, value) =>
                {
                    if (_disposed) return Task.CompletedTask;
                    ApplyMissionMissionMissionTaskModel(_model, value);
                    return Task.CompletedTask;
                },
                () => onChange?.Invoke()
            ));
            _unsubscribers.Add(__membershipMissionCompleteLoader.Subscribe(
                _gs2,
                _session,
                (_, _, value) =>
                {
                    if (_disposed) return Task.CompletedTask;
                    ApplyMembershipMissionComplete(_model, value);
                    return Task.CompletedTask;
                },
                () => onChange?.Invoke()
            ));
        }

        /// <summary>
        /// Invalidates cache and triggers a reload
        /// </summary>
        public void Invalidate()
        {
            ThrowIfDisposed();
            __transactionAcquireActionLoader.Invalidate(_gs2, _session);
            __missionMissionMissionTaskModelLoader.Invalidate(_gs2, _session);
            __membershipMissionCompleteLoader.Invalidate(_gs2, _session);
        }

        /// <summary>
        /// Throws an exception if already disposed
        /// </summary>
        private void ThrowIfDisposed()
        {
            if (_disposed)
            {
                throw new ObjectDisposedException(GetType().Name);
            }
        }

        /// <summary>
        /// Routes the navigation-time disposed guard to the owning lifecycle
        /// check. The read base hosts the single-fetch <c>Get{Child}</c> sugar
        /// (callable through <see cref="IReadOnlyMissionBinder"/>) and calls
        /// the base no-op hook; this override makes a disposed binder throw
        /// <c>ObjectDisposedException</c> from those navigation calls. Parent-side
        /// <c>Get{Child}s</c> sugar lives on this owning class and calls
        /// <c>ThrowIfDisposed</c> directly.
        /// </summary>
        protected override void ThrowIfDisposedForNavigation() => ThrowIfDisposed();

        /// <summary>
        /// Validates that the binder is ready to dispatch an action:
        /// not disposed and has been mounted (either via <c>CreateAsync</c> or a list factory).
        /// </summary>
        private void EnsureActionContext()
        {
            ThrowIfDisposed();
            if (!_mounted)
            {
                throw new InvalidOperationException(
                    $"{nameof(MissionBinder)} is not mounted. Use CreateAsync or one of the list factories.");
            }
        }

        /// <summary>
        /// Releases resources and unsubscribes
        /// </summary>
        public void Dispose()
        {
            if (_disposed)
            {
                return;
            }

            _disposed = true;

            foreach (var unsubscribe in _unsubscribers)
            {
                unsubscribe?.Invoke();
            }

            _unsubscribers.Clear();

            GC.SuppressFinalize(this);
        }

        #region Delegated actions
        public async Task Receive(Gs2.Unity.Gs2Mission.Model.EzConfig[]? config = null)
        {
            EnsureActionContext();
            await new Gs2Bind.Gs2Mission.MissionTaskModelLoader("Mission", _model.MissionCollection, _model.Id).ReceiveRewards(_gs2, _session, config: config);
        }
        #endregion

        #region Model composition
        /// <summary>
        /// Shared composition for the <c>__missionMissionMissionTaskModelLoader</c> source:
        /// writes the loaded value onto the model, or resets the covered
        /// properties when <c>source</c> is null. <c>MountAsync</c>,
        /// <c>Subscribe</c> and external stubs all route through this method.
        /// </summary>
        public static void ApplyMissionMissionMissionTaskModel(IMutableMission model, EzMissionTaskModel? source)
        {
            if (source != null)
            {
                // Skip primary id assignment
                model.TargetValue = source.TargetCounter.Value;
            }
            else
            {
                model.TargetValue = default;
            }
        }

        /// <summary>
        /// Shared composition for the <c>__membershipMissionCompleteLoader</c> source:
        /// writes the loaded value onto the model, or resets the covered
        /// properties when <c>source</c> is null. <c>MountAsync</c>,
        /// <c>Subscribe</c> and external stubs all route through this method.
        /// </summary>
        public static void ApplyMembershipMissionComplete(IMutableMission model, EzComplete? source)
        {
            if (source != null)
            {
                model.Completed = source.CompletedMissionTaskNames?.Contains(model.Id.ToString() ?? string.Empty) ?? false;
                model.Received = source.ReceivedMissionTaskNames?.Contains(model.Id.ToString() ?? string.Empty) ?? false;
            }
            else
            {
                model.Completed = default;
                model.Received = default;
            }
        }
        #endregion

    }
}
