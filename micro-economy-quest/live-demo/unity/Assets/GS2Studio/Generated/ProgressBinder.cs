// <auto-generated>
//     This code was generated by CSharpBinderGeneratorService.
//     Do not modify this file directly.
// </auto-generated>

#nullable enable

using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using Cysharp.Threading.Tasks;

using Gs2.Unity.Core;
using Gs2.Unity.Util;
using GS2Studio.Generated.Quest;
using GS2Studio.Generated.QuestCollection;
using Gs2.Unity.Gs2Quest.Model;
using Gs2Bind.Gs2Quest;

namespace GS2Studio.Generated.Progress
{
    /// <summary>
    /// Pure read contract for <see cref="ProgressBinder"/>: the read-only
    /// model surface (<see cref="Progress"/>) plus the single-fetch
    /// read navigation. Substitutable seam — code holding a binder for reads
    /// can be faked against this interface. No server-side operations and no
    /// lifecycle (Subscribe/Dispose/Mount).
    /// </summary>
    public interface IReadOnlyProgressBinder : Progress
    {
    }

    /// <summary>
    /// Actionable contract for <see cref="ProgressBinder"/>: the read contract
    /// plus the delegated-action dispatchers (server-side operations). This is
    /// the element type yielded by the companion Collection (enumeration /
    /// <c>Get</c> / <c>Where*</c>), so actions stay callable from collection
    /// results. Still no lifecycle (Subscribe/Dispose/Mount).
    /// </summary>
    public interface IActionableProgressBinder : IReadOnlyProgressBinder
    {
        Task Complete(Gs2.Unity.Gs2Quest.Model.EzReward[]? rewards = null, Gs2.Unity.Gs2Quest.Model.EzConfig[]? config = null);
    }

    /// <summary>
    /// Owning contract for <see cref="ProgressBinder"/>: adds the lifecycle
    /// surface (Subscribe / Invalidate / Dispose) over <see cref="IActionableProgressBinder"/>.
    /// Returned by the binder factory; a fake implementation substitutes the
    /// whole binder.
    /// </summary>
    public interface IProgressBinder : IActionableProgressBinder, IDisposable
    {
        void Subscribe(Action? onChange = null);
        void Invalidate();
    }

    /// <summary>
    /// Lifecycle-free base for <see cref="ProgressBinder"/>.
    /// Owns <c>_model</c> / <c>_gs2</c> / <c>_session</c> and implements the
    /// read-only <see cref="Progress"/> contract by forwarding each
    /// model property to <c>_model</c> (so consumers read <c>binder.{Prop}</c>
    /// directly). This lets binders reached through a non-owning view
    /// (collection <c>Get</c> / <c>Where*</c> / enumeration) still read their
    /// model. Never exposes <c>Dispose</c>, <c>Subscribe</c>, or
    /// <c>MountAsync</c>; the disposed-guard hook is a virtual no-op overridden
    /// by the owning derived class. Delegated actions live on the owning
    /// derived class (every binder instance is the owning type).
    /// </summary>
    public class ReadOnlyProgressBinder : IReadOnlyProgressBinder
    {
        // `private protected` because the mutable model type is internal; only
        // the same-assembly owning derived binder reads/writes this field.
        private protected readonly MutableProgress _model;
        protected readonly Gs2Domain _gs2;
        protected readonly IGameSession _session;

        /// <summary>
        /// Internal mutable view of the bound model. Reconcile paths on the
        /// owning collection write through this; external consumers only see
        /// the read-only <c>Progress</c> surface this binder implements.
        /// </summary>
        internal MutableProgress MutableModel => _model;
        /// <inheritdoc cref="Progress.Id" />
        public ProgressId Id => _model.Id;
        /// <inheritdoc cref="Progress.InProgress" />
        public bool InProgress => _model.InProgress;
        /// <inheritdoc cref="Progress.Quest" />
        public QuestId Quest => _model.Quest;
        /// <inheritdoc cref="Progress.QuestCollection" />
        public QuestCollectionId QuestCollection => _model.QuestCollection;
        /// <inheritdoc cref="Progress.Rewards" />
        public IReadOnlyList<GS2Studio.Generated.ProgressReward.ProgressReward>? Rewards => _model.Rewards;
        /// <summary>
        /// Base constructor. Stores the bound model + service handles on the
        /// protected fields shared with the owning derived class. `private
        /// protected` because the mutable model parameter is internal (only the
        /// same-assembly owning binder constructs through here).
        /// </summary>
        private protected ReadOnlyProgressBinder(MutableProgress model, Gs2Domain gs2, IGameSession session)
        {
            _model = model ?? throw new ArgumentNullException(nameof(model));
            _gs2 = gs2 ?? throw new ArgumentNullException(nameof(gs2));
            _session = session ?? throw new ArgumentNullException(nameof(session));
        }

        /// <summary>
        /// Disposed-guard hook for navigation declared on this read base. The
        /// single-fetch <c>Get{Child}</c> sugar lives here (so it is callable
        /// through the read contract <see cref="IReadOnlyProgressBinder"/>) and
        /// calls this hook. The base is a no-op; the owning derived class
        /// overrides it to route to <c>ThrowIfDisposed</c>, so a disposed binder
        /// still throws (every binder instance is the owning derived type).
        /// Parent-side <c>Get{Child}s</c> sugar stays on the owning class — it
        /// owns the lazily-built child Collection root it disposes.
        /// </summary>
        protected virtual void ThrowIfDisposedForNavigation() { }
    }

    /// <summary>
    /// Binder for Progress
    /// Binds GS2 resource values to the model and automatically reflects server-side changes
    /// </summary>
    public sealed class ProgressBinder : ReadOnlyProgressBinder, IProgressBinder
    {
        private readonly List<Action> _unsubscribers = new List<Action>();
        private bool _disposed;
        internal bool _mounted;

        private readonly Gs2Bind.Gs2Quest.ProgressLoader __userdataQuestQuestQuestModelLoader;

        /// <summary>
        /// Internal constructor. External construction must go through <c>CreateAsync</c>
        /// or one of the list static factories on the companion Collection class.
        /// </summary>
        internal ProgressBinder(
            MutableProgress model,
            Gs2Domain gs2,
            IGameSession session
        ) : base(model, gs2, session)
        {
            __userdataQuestQuestQuestModelLoader = new Gs2Bind.Gs2Quest.ProgressLoader("Quest");
        }

        /// <summary>
        /// Creates a Progress instance from identity parameters. The
        /// returned model carries the identity state required by loaders
        /// (`_model.Id` and any `_model.{Prop}` references in loader constructors).
        /// </summary>
        internal static MutableProgress CreateModel(ProgressId id)
        {
            var model = new MutableProgress();
            model.Id = id;
            return model;
        }

        /// <summary>
        /// Creates a ProgressBinder and mounts it to GS2 resources.
        /// This is the only external construction path for mount-based usage.
        /// </summary>
        public static async Task<ProgressBinder> CreateAsync(
            ProgressId id,
            Gs2Domain gs2,
            IGameSession session,
            CancellationToken cancellationToken = default)
        {
            var model = CreateModel(id);
            var binder = new ProgressBinder(model, gs2, session);
            await binder.MountAsync(cancellationToken);
            return binder;
        }

        /// <summary>
        /// Loads GS2 resource values and mounts them to the model
        /// </summary>
        /// <param name="cancellationToken">Cancellation token</param>
        public async Task MountAsync(CancellationToken cancellationToken = default)
        {
            ThrowIfDisposed();
            cancellationToken.ThrowIfCancellationRequested();
            var _userdataQuestQuestQuestModel = await __userdataQuestQuestQuestModelLoader.LoadOrNull(_gs2, _session);
            cancellationToken.ThrowIfCancellationRequested();
            ApplyUserdataQuestQuestQuestModel(_model, _userdataQuestQuestQuestModel);
            var __rewardsItems = new List<GS2Studio.Generated.ProgressReward.ProgressReward>();
            if (_userdataQuestQuestQuestModel?.Rewards != null)
            {
                foreach (var __element in _userdataQuestQuestQuestModel.Rewards)
                {
                    if (__element == null) continue;
                    if (__element.ItemId == null) continue;
                    var row = new GS2Studio.Generated.ProgressReward.MutableProgressReward();
                    row.Id = new GS2Studio.Generated.ProgressReward.ProgressRewardId(string.Empty);
                    row.ItemId = __element.ItemId;
                    row.Value = __element.Value;
                    __rewardsItems.Add(row);
                }
            }
            _model.Rewards = __rewardsItems;
            _mounted = true;
        }

        /// <summary>
        /// Subscribes to GS2 resource changes and automatically updates the model
        /// </summary>
        /// <param name="onChange">Callback on change (optional)</param>
        public void Subscribe(Action? onChange = null)
        {
            ThrowIfDisposed();
            _unsubscribers.Add(__userdataQuestQuestQuestModelLoader.Subscribe(
                _gs2,
                _session,
                (_, _, value) =>
                {
                    if (_disposed) return Task.CompletedTask;
                    ApplyUserdataQuestQuestQuestModel(_model, value);
                    var __rewardsItems = new List<GS2Studio.Generated.ProgressReward.ProgressReward>();
                    if (value?.Rewards != null)
                    {
                        foreach (var __element in value.Rewards)
                        {
                            if (__element == null) continue;
                            if (__element.ItemId == null) continue;
                            var row = new GS2Studio.Generated.ProgressReward.MutableProgressReward();
                            row.Id = new GS2Studio.Generated.ProgressReward.ProgressRewardId(string.Empty);
                            row.ItemId = __element.ItemId;
                            row.Value = __element.Value;
                            __rewardsItems.Add(row);
                        }
                    }
                    _model.Rewards = __rewardsItems;
                    return Task.CompletedTask;
                },
                () => onChange?.Invoke()
            ));
        }

        /// <summary>
        /// Invalidates cache and triggers a reload
        /// </summary>
        public void Invalidate()
        {
            ThrowIfDisposed();
            __userdataQuestQuestQuestModelLoader.Invalidate(_gs2, _session);
        }

        /// <summary>
        /// Throws an exception if already disposed
        /// </summary>
        private void ThrowIfDisposed()
        {
            if (_disposed)
            {
                throw new ObjectDisposedException(GetType().Name);
            }
        }

        /// <summary>
        /// Routes the navigation-time disposed guard to the owning lifecycle
        /// check. The read base hosts the single-fetch <c>Get{Child}</c> sugar
        /// (callable through <see cref="IReadOnlyProgressBinder"/>) and calls
        /// the base no-op hook; this override makes a disposed binder throw
        /// <c>ObjectDisposedException</c> from those navigation calls. Parent-side
        /// <c>Get{Child}s</c> sugar lives on this owning class and calls
        /// <c>ThrowIfDisposed</c> directly.
        /// </summary>
        protected override void ThrowIfDisposedForNavigation() => ThrowIfDisposed();

        /// <summary>
        /// Validates that the binder is ready to dispatch an action:
        /// not disposed and has been mounted (either via <c>CreateAsync</c> or a list factory).
        /// </summary>
        private void EnsureActionContext()
        {
            ThrowIfDisposed();
            if (!_mounted)
            {
                throw new InvalidOperationException(
                    $"{nameof(ProgressBinder)} is not mounted. Use CreateAsync or one of the list factories.");
            }
        }

        /// <summary>
        /// Releases resources and unsubscribes
        /// </summary>
        public void Dispose()
        {
            if (_disposed)
            {
                return;
            }

            _disposed = true;

            foreach (var unsubscribe in _unsubscribers)
            {
                unsubscribe?.Invoke();
            }

            _unsubscribers.Clear();

            GC.SuppressFinalize(this);
        }

        #region Delegated actions
        public async Task Complete(Gs2.Unity.Gs2Quest.Model.EzReward[]? rewards = null, Gs2.Unity.Gs2Quest.Model.EzConfig[]? config = null)
        {
            EnsureActionContext();
            await new Gs2Bind.Gs2Quest.ProgressLoader("Quest").End(_gs2, _session, isComplete: true, rewards: rewards, config: config);
        }
        #endregion

        #region Model composition
        /// <summary>
        /// Shared composition for the <c>__userdataQuestQuestQuestModelLoader</c> source:
        /// writes the loaded value onto the model, or resets the covered
        /// properties when <c>source</c> is null. <c>MountAsync</c>,
        /// <c>Subscribe</c> and external stubs all route through this method.
        /// </summary>
        public static void ApplyUserdataQuestQuestQuestModel(IMutableProgress model, Gs2.Unity.Gs2Quest.Model.EzProgress? source)
        {
            model.InProgress = source != null;
            if (source != null)
            {
                model.QuestCollection = string.IsNullOrEmpty(source.QuestModelId) || string.IsNullOrEmpty(Gs2.Gs2Quest.Model.QuestModel.GetQuestGroupNameFromGrn(source.QuestModelId)) ? new QuestCollectionId(string.Empty) : (QuestCollectionId)Gs2.Gs2Quest.Model.QuestModel.GetQuestGroupNameFromGrn(source.QuestModelId);
                model.Quest = string.IsNullOrEmpty(source.QuestModelId) || string.IsNullOrEmpty(Gs2.Gs2Quest.Model.QuestModel.GetQuestNameFromGrn(source.QuestModelId)) ? new QuestId(string.Empty) : (QuestId)Gs2.Gs2Quest.Model.QuestModel.GetQuestNameFromGrn(source.QuestModelId);
            }
            else
            {
                model.QuestCollection = new QuestCollectionId(string.Empty);
                model.Quest = new QuestId(string.Empty);
            }
        }
        #endregion

    }
}
