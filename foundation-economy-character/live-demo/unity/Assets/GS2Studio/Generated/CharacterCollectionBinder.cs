// <auto-generated>
//     This code was generated by CSharpBinderGeneratorService.
//     Do not modify this file directly.
// </auto-generated>

#nullable enable

using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using Cysharp.Threading.Tasks;

using Gs2.Unity.Core;
using Gs2.Unity.Util;
using Gs2.Unity.Gs2Inventory.Model;
using Gs2Bind.Gs2Inventory;

namespace GS2Studio.Generated.CharacterCollection
{
    /// <summary>
    /// Pure read contract for <see cref="CharacterCollectionBinder"/>: the read-only
    /// model surface (<see cref="CharacterCollection"/>) plus the single-fetch
    /// read navigation. Substitutable seam — code holding a binder for reads
    /// can be faked against this interface. No server-side operations and no
    /// lifecycle (Subscribe/Dispose/Mount).
    /// </summary>
    public interface IReadOnlyCharacterCollectionBinder : CharacterCollection
    {
    }

    /// <summary>
    /// Actionable contract for <see cref="CharacterCollectionBinder"/>: the read contract
    /// plus the delegated-action dispatchers (server-side operations). This is
    /// the element type yielded by the companion Collection (enumeration /
    /// <c>Get</c> / <c>Where*</c>), so actions stay callable from collection
    /// results. Still no lifecycle (Subscribe/Dispose/Mount).
    /// </summary>
    public interface IActionableCharacterCollectionBinder : IReadOnlyCharacterCollectionBinder
    {
    }

    /// <summary>
    /// Owning contract for <see cref="CharacterCollectionBinder"/>: adds the lifecycle
    /// surface (Subscribe / Invalidate / Dispose) over <see cref="IActionableCharacterCollectionBinder"/>.
    /// Returned by the binder factory; a fake implementation substitutes the
    /// whole binder.
    /// </summary>
    public interface ICharacterCollectionBinder : IActionableCharacterCollectionBinder, IDisposable
    {
        void Subscribe(Action? onChange = null);
        void Invalidate();
    }

    /// <summary>
    /// Lifecycle-free base for <see cref="CharacterCollectionBinder"/>.
    /// Owns <c>_model</c> / <c>_gs2</c> / <c>_session</c> and implements the
    /// read-only <see cref="CharacterCollection"/> contract by forwarding each
    /// model property to <c>_model</c> (so consumers read <c>binder.{Prop}</c>
    /// directly). This lets binders reached through a non-owning view
    /// (collection <c>Get</c> / <c>Where*</c> / enumeration) still read their
    /// model. Never exposes <c>Dispose</c>, <c>Subscribe</c>, or
    /// <c>MountAsync</c>; the disposed-guard hook is a virtual no-op overridden
    /// by the owning derived class. Delegated actions live on the owning
    /// derived class (every binder instance is the owning type).
    /// </summary>
    public class ReadOnlyCharacterCollectionBinder : IReadOnlyCharacterCollectionBinder
    {
        // `private protected` because the mutable model type is internal; only
        // the same-assembly owning derived binder reads/writes this field.
        private protected readonly MutableCharacterCollection _model;
        protected readonly Gs2Domain _gs2;
        protected readonly IGameSession _session;

        /// <summary>
        /// Internal mutable view of the bound model. Reconcile paths on the
        /// owning collection write through this; external consumers only see
        /// the read-only <c>CharacterCollection</c> surface this binder implements.
        /// </summary>
        internal MutableCharacterCollection MutableModel => _model;
        /// <inheritdoc cref="CharacterCollection.Id" />
        public CharacterCollectionId Id => _model.Id;
        /// <inheritdoc cref="CharacterCollection.MaximumCapacity" />
        public int MaximumCapacity => _model.MaximumCapacity;
        /// <inheritdoc cref="CharacterCollection.DefaultCapacity" />
        public int DefaultCapacity => _model.DefaultCapacity;
        /// <inheritdoc cref="CharacterCollection.CurrentCpacityUsage" />
        public int CurrentCpacityUsage => _model.CurrentCpacityUsage;
        /// <inheritdoc cref="CharacterCollection.CurrentCpacity" />
        public int CurrentCpacity => _model.CurrentCpacity;
        /// <summary>
        /// Base constructor. Stores the bound model + service handles on the
        /// protected fields shared with the owning derived class. `private
        /// protected` because the mutable model parameter is internal (only the
        /// same-assembly owning binder constructs through here).
        /// </summary>
        private protected ReadOnlyCharacterCollectionBinder(MutableCharacterCollection model, Gs2Domain gs2, IGameSession session)
        {
            _model = model ?? throw new ArgumentNullException(nameof(model));
            _gs2 = gs2 ?? throw new ArgumentNullException(nameof(gs2));
            _session = session ?? throw new ArgumentNullException(nameof(session));
        }

        /// <summary>
        /// Disposed-guard hook for navigation declared on this read base. The
        /// single-fetch <c>Get{Child}</c> sugar lives here (so it is callable
        /// through the read contract <see cref="IReadOnlyCharacterCollectionBinder"/>) and
        /// calls this hook. The base is a no-op; the owning derived class
        /// overrides it to route to <c>ThrowIfDisposed</c>, so a disposed binder
        /// still throws (every binder instance is the owning derived type).
        /// Parent-side <c>Get{Child}s</c> sugar stays on the owning class — it
        /// owns the lazily-built child Collection root it disposes.
        /// </summary>
        protected virtual void ThrowIfDisposedForNavigation() { }
    }

    /// <summary>
    /// Binder for CharacterCollection
    /// Binds GS2 resource values to the model and automatically reflects server-side changes
    /// </summary>
    public sealed class CharacterCollectionBinder : ReadOnlyCharacterCollectionBinder, ICharacterCollectionBinder
    {
        private readonly List<Action> _unsubscribers = new List<Action>();
        private bool _disposed;
        internal bool _mounted;

        private readonly InventoryModelLoader __inventoryCharacterNamespaceInventoryModelLoader;
        private readonly InventoryLoader __userdataInventoryCharacterInventoryModelLoader;

        /// <summary>
        /// Internal constructor. External construction must go through <c>CreateAsync</c>
        /// or one of the list static factories on the companion Collection class.
        /// </summary>
        internal CharacterCollectionBinder(
            MutableCharacterCollection model,
            Gs2Domain gs2,
            IGameSession session
        ) : base(model, gs2, session)
        {
            __inventoryCharacterNamespaceInventoryModelLoader = new InventoryModelLoader("Character", "Character");
            __userdataInventoryCharacterInventoryModelLoader = new InventoryLoader("Character", "Character");
        }

        /// <summary>
        /// Creates a CharacterCollection instance from identity parameters. The
        /// returned model carries the identity state required by loaders
        /// (`_model.Id` and any `_model.{Prop}` references in loader constructors).
        /// </summary>
        internal static MutableCharacterCollection CreateModel(CharacterCollectionId id)
        {
            var model = new MutableCharacterCollection();
            model.Id = id;
            return model;
        }

        /// <summary>
        /// Creates a CharacterCollectionBinder and mounts it to GS2 resources.
        /// This is the only external construction path for mount-based usage.
        /// </summary>
        public static async Task<CharacterCollectionBinder> CreateAsync(
            CharacterCollectionId id,
            Gs2Domain gs2,
            IGameSession session,
            CancellationToken cancellationToken = default)
        {
            var model = CreateModel(id);
            var binder = new CharacterCollectionBinder(model, gs2, session);
            await binder.MountAsync(cancellationToken);
            return binder;
        }

        /// <summary>
        /// Loads GS2 resource values and mounts them to the model
        /// </summary>
        /// <param name="cancellationToken">Cancellation token</param>
        public async Task MountAsync(CancellationToken cancellationToken = default)
        {
            ThrowIfDisposed();
            cancellationToken.ThrowIfCancellationRequested();
            var _inventoryCharacterNamespaceInventoryModel = await __inventoryCharacterNamespaceInventoryModelLoader.Load(_gs2, _session);
            cancellationToken.ThrowIfCancellationRequested();
            ApplyInventoryCharacterNamespaceInventoryModel(_model, _inventoryCharacterNamespaceInventoryModel);
            var _userdataInventoryCharacterInventoryModel = await __userdataInventoryCharacterInventoryModelLoader.Load(_gs2, _session);
            cancellationToken.ThrowIfCancellationRequested();
            ApplyUserdataInventoryCharacterInventoryModel(_model, _userdataInventoryCharacterInventoryModel);
            _mounted = true;
        }

        /// <summary>
        /// Subscribes to GS2 resource changes and automatically updates the model
        /// </summary>
        /// <param name="onChange">Callback on change (optional)</param>
        public void Subscribe(Action? onChange = null)
        {
            ThrowIfDisposed();
            _unsubscribers.Add(__inventoryCharacterNamespaceInventoryModelLoader.Subscribe(
                _gs2,
                _session,
                (_, _, value) =>
                {
                    if (_disposed) return Task.CompletedTask;
                    ApplyInventoryCharacterNamespaceInventoryModel(_model, value);
                    return Task.CompletedTask;
                },
                () => onChange?.Invoke()
            ));
            _unsubscribers.Add(__userdataInventoryCharacterInventoryModelLoader.Subscribe(
                _gs2,
                _session,
                (_, _, value) =>
                {
                    if (_disposed) return Task.CompletedTask;
                    ApplyUserdataInventoryCharacterInventoryModel(_model, value);
                    return Task.CompletedTask;
                },
                () => onChange?.Invoke()
            ));
        }

        /// <summary>
        /// Invalidates cache and triggers a reload
        /// </summary>
        public void Invalidate()
        {
            ThrowIfDisposed();
            __inventoryCharacterNamespaceInventoryModelLoader.Invalidate(_gs2, _session);
            __userdataInventoryCharacterInventoryModelLoader.Invalidate(_gs2, _session);
        }

        /// <summary>
        /// Throws an exception if already disposed
        /// </summary>
        private void ThrowIfDisposed()
        {
            if (_disposed)
            {
                throw new ObjectDisposedException(GetType().Name);
            }
        }

        /// <summary>
        /// Routes the navigation-time disposed guard to the owning lifecycle
        /// check. The read base hosts the single-fetch <c>Get{Child}</c> sugar
        /// (callable through <see cref="IReadOnlyCharacterCollectionBinder"/>) and calls
        /// the base no-op hook; this override makes a disposed binder throw
        /// <c>ObjectDisposedException</c> from those navigation calls. Parent-side
        /// <c>Get{Child}s</c> sugar lives on this owning class and calls
        /// <c>ThrowIfDisposed</c> directly.
        /// </summary>
        protected override void ThrowIfDisposedForNavigation() => ThrowIfDisposed();

        /// <summary>
        /// Validates that the binder is ready to dispatch an action:
        /// not disposed and has been mounted (either via <c>CreateAsync</c> or a list factory).
        /// </summary>
        private void EnsureActionContext()
        {
            ThrowIfDisposed();
            if (!_mounted)
            {
                throw new InvalidOperationException(
                    $"{nameof(CharacterCollectionBinder)} is not mounted. Use CreateAsync or one of the list factories.");
            }
        }

        /// <summary>
        /// Releases resources and unsubscribes
        /// </summary>
        public void Dispose()
        {
            if (_disposed)
            {
                return;
            }

            _disposed = true;

            foreach (var unsubscribe in _unsubscribers)
            {
                unsubscribe?.Invoke();
            }

            _unsubscribers.Clear();

            GC.SuppressFinalize(this);
        }

        #region Model composition
        /// <summary>
        /// Shared composition for the <c>__inventoryCharacterNamespaceInventoryModelLoader</c> source:
        /// writes the loaded value onto the model, or resets the covered
        /// properties when <c>source</c> is null. <c>MountAsync</c>,
        /// <c>Subscribe</c> and external stubs all route through this method.
        /// </summary>
        public static void ApplyInventoryCharacterNamespaceInventoryModel(IMutableCharacterCollection model, EzInventoryModel? source)
        {
            if (source != null)
            {
                model.DefaultCapacity = source.InitialCapacity;
                model.MaximumCapacity = source.MaxCapacity;
            }
            else
            {
                model.DefaultCapacity = default;
                model.MaximumCapacity = default;
            }
        }

        /// <summary>
        /// Shared composition for the <c>__userdataInventoryCharacterInventoryModelLoader</c> source:
        /// writes the loaded value onto the model, or resets the covered
        /// properties when <c>source</c> is null. <c>MountAsync</c>,
        /// <c>Subscribe</c> and external stubs all route through this method.
        /// </summary>
        public static void ApplyUserdataInventoryCharacterInventoryModel(IMutableCharacterCollection model, EzInventory? source)
        {
            if (source != null)
            {
                model.CurrentCpacityUsage = source.CurrentInventoryCapacityUsage;
                model.CurrentCpacity = source.CurrentInventoryMaxCapacity;
            }
            else
            {
                model.CurrentCpacityUsage = default;
                model.CurrentCpacity = default;
            }
        }
        #endregion

    }
}
