// <auto-generated>
//     This code was generated by CSharpBinderGeneratorService.
//     Do not modify this file directly.
// </auto-generated>

#nullable enable

using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;

using Gs2.Unity.Core;
using Gs2.Unity.Util;
using GS2Studio.Generated.EquipmentEnchant;

namespace GS2Studio.Generated.EquipmentEnchantOption
{
    /// <summary>
    /// Public read contract for the EquipmentEnchantOption binder collection: the
    /// stable <see cref="IReadOnlyList{T}"/> over the actionable (non-owning)
    /// element binders. Access is index/enumeration based — the domain id is
    /// not unique per row (multiple backing rows may fan out to instances
    /// sharing one id), so id lookup is a caller-side LINQ concern.
    /// Substitutable seam for code that only enumerates binders.
    /// </summary>
    public interface IReadOnlyEquipmentEnchantOptionBinderCollection : IReadOnlyList<IActionableEquipmentEnchantOptionBinder>
    {
        IReadOnlyEquipmentEnchantOptionBinderCollection WhereEnchant(EquipmentEnchantId enchant);
    }

    /// <summary>
    /// Owning contract for the EquipmentEnchantOption binder collection: adds the
    /// lifecycle + mount/subscribe surface and the owning-binder wiring events
    /// over <see cref="IReadOnlyEquipmentEnchantOptionBinderCollection"/>. Returned by the binder
    /// factory; a fake implementation substitutes the whole collection.
    /// </summary>
    public interface IEquipmentEnchantOptionBinderCollection : IReadOnlyEquipmentEnchantOptionBinderCollection, IDisposable
    {
        IComparer<IReadOnlyEquipmentEnchantOptionBinder> Comparer { get; set; }
        event Action<IEquipmentEnchantOptionBinder> ItemAdded;
        event Action<IEquipmentEnchantOptionBinder> ItemRemoved;
        void Invalidate();
        Task MountFromOverlayAsync(CancellationToken cancellationToken = default);
    }

    /// <summary>
    /// Lifecycle-free base for <see cref="EquipmentEnchantOptionBinderCollection"/>.
    /// Exposes the <see cref="IReadOnlyList{T}"/> contract over
    /// <see cref="IActionableEquipmentEnchantOptionBinder"/> and owns the
    /// <c>Where{RefProp}</c> reference-navigation filter helpers. Filter view
    /// instances reach the owning root through the internal ctor + the
    /// <c>RegisterDerivedView</c> hook. Never exposes <c>Dispose</c>,
    /// <c>Mount*</c>, or <c>Subscribe*</c>.
    /// </summary>
    public class ReadOnlyEquipmentEnchantOptionBinderCollection : IReadOnlyEquipmentEnchantOptionBinderCollection
    {
        // Filter-view backing state. The owning derived class leaves these
        // null and overrides every observation member, so the `_cache == null`
        // / `_dead` branches only kick in for filter views and the static
        // `Empty` instance.
        private readonly EquipmentEnchantOptionBinderCollection? _rootSource;
        private readonly Predicate<IReadOnlyEquipmentEnchantOptionBinder>? _predicate;
        private readonly List<IActionableEquipmentEnchantOptionBinder>? _cache;
        private bool _dead;

        // Shared sentinel list backing the `_dead` / `Empty` enumeration
        // paths. Reused so a dead view does not allocate per access.
        private static readonly List<IActionableEquipmentEnchantOptionBinder> EmptyList = new List<IActionableEquipmentEnchantOptionBinder>(0);

        /// <summary>
        /// Sentinel non-owning empty collection. Returned by <c>Where*</c>
        /// fast-exit paths (disposed view, dead view, default-id, no root).
        /// </summary>
        internal static ReadOnlyEquipmentEnchantOptionBinderCollection Empty { get; } = new ReadOnlyEquipmentEnchantOptionBinderCollection();

        /// <summary>
        /// Parameterless ctor for the owning derived class and the
        /// <see cref="Empty"/> sentinel. The view state stays null so the
        /// fallback `_cache == null` branch keeps the sentinel safe.
        /// </summary>
        protected ReadOnlyEquipmentEnchantOptionBinderCollection() { }

        /// <summary>
        /// Filter-view ctor. Registers the view with the owning root so
        /// subsequent source mutations refresh this view's cache.
        /// </summary>
        internal ReadOnlyEquipmentEnchantOptionBinderCollection(
            EquipmentEnchantOptionBinderCollection rootSource,
            Predicate<IReadOnlyEquipmentEnchantOptionBinder> predicate)
        {
            _rootSource = rootSource;
            _predicate = predicate;
            _cache = new List<IActionableEquipmentEnchantOptionBinder>();
            rootSource.RegisterDerivedView(this);
            RefreshCache();
        }

        /// <summary>Element count. Derived owning collection overrides.</summary>
        public virtual int Count => (_dead || _cache == null) ? 0 : _cache.Count;

        /// <summary>Indexer. Derived owning collection overrides.</summary>
        public virtual IActionableEquipmentEnchantOptionBinder this[int index]
            => (_dead || _cache == null) ? EmptyList[index] : _cache[index];

        /// <summary>Enumerator. Derived owning collection overrides.</summary>
        public virtual IEnumerator<IActionableEquipmentEnchantOptionBinder> GetEnumerator()
            => (_dead || _cache == null) ? EmptyList.GetEnumerator() : _cache.GetEnumerator();

        IEnumerator IEnumerable.GetEnumerator() => GetEnumerator();

        /// <summary>
        /// Returns the owning root collection backing this view. Derived
        /// owning collection returns itself; filter views return the source
        /// they were constructed against.
        /// </summary>
        protected virtual EquipmentEnchantOptionBinderCollection? GetRootSource() => _rootSource;

        /// <summary>
        /// Disposed-guard hook used by reference-navigation methods. Base
        /// implementation is a no-op so filter views and the <see cref="Empty"/>
        /// sentinel do not need to know about lifecycle; the owning derived
        /// class overrides this to call <c>ThrowIfDisposed</c>.
        /// </summary>
        protected virtual void ThrowIfDisposedForNavigation() { }

        /// <summary>
        /// Rebuilds this view's cache from the root collection's currently
        /// owned binders. No-op when the view is dead, lacks a root source,
        /// or is the parameterless <see cref="Empty"/> sentinel.
        /// </summary>
        internal void RefreshCache()
        {
            if (_dead || _rootSource == null || _cache == null || _predicate == null) return;
            _cache.Clear();
            foreach (var b in _rootSource)
            {
                if (_predicate(b)) _cache.Add(b);
            }
        }

        /// <summary>
        /// Marks the view dead. Subsequent observation returns the empty
        /// sentinel and `Where*` short-circuits to <see cref="Empty"/>.
        /// </summary>
        internal void MarkDead()
        {
            _dead = true;
            _cache?.Clear();
        }


        #region Reference navigation
        public IReadOnlyEquipmentEnchantOptionBinderCollection WhereEnchant(EquipmentEnchantId enchant)
        {
            ThrowIfDisposedForNavigation();
            if (_dead) return Empty;
            if (EqualityComparer<EquipmentEnchantId>.Default.Equals(enchant, default)) return Empty;
            var root = GetRootSource();
            if (root == null) return Empty;
            var existing = _predicate;
            Predicate<IReadOnlyEquipmentEnchantOptionBinder> composed = existing == null
                ? (Predicate<IReadOnlyEquipmentEnchantOptionBinder>)(b => MatchEnchant(b, enchant))
                : b => existing(b) && MatchEnchant(b, enchant);
            return new ReadOnlyEquipmentEnchantOptionBinderCollection(root, composed);
        }
        private static bool MatchEnchant(IReadOnlyEquipmentEnchantOptionBinder b, EquipmentEnchantId enchant)
            => EqualityComparer<EquipmentEnchantId>.Default.Equals(b.Enchant, enchant);
        #endregion

    }

    /// <summary>
    /// Collection binder for EquipmentEnchantOption.
    /// Wraps a list of EquipmentEnchantOptionBinder elements and reflects list-level
    /// changes (additions / removals) and per-element changes from the GS2
    /// data sources, using identity-based reconciliation so element binder
    /// instances stay stable across list updates.
    /// </summary>
    public sealed class EquipmentEnchantOptionBinderCollection : ReadOnlyEquipmentEnchantOptionBinderCollection, IEquipmentEnchantOptionBinderCollection
    {
        private readonly Gs2Domain _gs2;
        private readonly IGameSession _session;

        // Owning binders are internal: consumers read the collection as the
        // non-owning IReadOnlyList<IActionableEquipmentEnchantOptionBinder> contract; only the
        // collection (and the wiring ItemAdded/ItemRemoved events) touch the
        // owning EquipmentEnchantOptionBinder so lifecycle (Subscribe/Dispose) never leaks.
        private readonly List<EquipmentEnchantOptionBinder> _binders = new List<EquipmentEnchantOptionBinder>();
        // Reconcile index keyed by the per-item ROW KEY (backing row identity),
        // not by Model.Id: the domain id may legitimately repeat across rows
        // (fan-out), while the row key is unique within one collection.
        private readonly Dictionary<string, EquipmentEnchantOptionBinder> _bindersByRowKey = new Dictionary<string, EquipmentEnchantOptionBinder>();
        private readonly List<Action> _unsubscribers = new List<Action>();
        // Weakly-referenced filter views so view-only consumers can be GC'd
        // without forcing the owning collection to hold them alive. Dead
        // entries are pruned during `RaiseSourceChanged`.
        private readonly List<WeakReference<ReadOnlyEquipmentEnchantOptionBinderCollection>> _derivedViews = new List<WeakReference<ReadOnlyEquipmentEnchantOptionBinderCollection>>();
        private bool _disposed;
        private bool _mounted;
        private Action? _onChange;

        // Sort comparers — `_configuredComparer` is what consumers observe via
        // the `Comparer` getter; `_effectiveComparer` wraps it with an Id
        // tie-break for distinct IDs. Same-ID rows can still compare equal;
        // SortBinders uses stable OrderBy to preserve their input order.
        private IComparer<IReadOnlyEquipmentEnchantOptionBinder> _configuredComparer = EquipmentEnchantOptionBinderComparer.Default;
        private IComparer<IReadOnlyEquipmentEnchantOptionBinder> _effectiveComparer =
            new EquipmentEnchantOptionBinderIdTieBreakComparer(EquipmentEnchantOptionBinderComparer.Default);

        /// <summary>
        /// User-facing comparer driving the binder sort order. Reading returns
        /// the value last assigned (no wrapper leakage). Setting installs an
        /// Id-tie-break wrapper internally so distinct Model.Id values have a
        /// deterministic fallback when the supplied comparer returns 0. Rows
        /// with the same Id remain equal and rely on SortBinders' stable
        /// OrderBy. Typed over the non-owning IReadOnlyEquipmentEnchantOptionBinder so
        /// it never exposes the owning binder.
        /// </summary>
        public IComparer<IReadOnlyEquipmentEnchantOptionBinder> Comparer
        {
            get => _configuredComparer;
            set
            {
                var c = value ?? throw new ArgumentNullException(nameof(value));
                _configuredComparer = c;
                _effectiveComparer = new EquipmentEnchantOptionBinderIdTieBreakComparer(c);
                SortBinders();
                _onChange?.Invoke();
            }
        }

        /// <summary>Element count over the owning binder list.</summary>
        public override int Count => _binders.Count;

        /// <summary>
        /// Indexer over the owning binder list. Return type is the non-owning
        /// IActionableEquipmentEnchantOptionBinder contract — the public surface never exposes
        /// the owning binder.
        /// </summary>
        public override IActionableEquipmentEnchantOptionBinder this[int index] => _binders[index];

        /// <summary>
        /// Enumerator over the owning binder list, yielding the non-owning
        /// IActionableEquipmentEnchantOptionBinder contract.
        /// </summary>
        public override IEnumerator<IActionableEquipmentEnchantOptionBinder> GetEnumerator() => _binders.GetEnumerator();

        /// <summary>
        /// Returns the owning root collection — itself. Filter views call
        /// this on their source to normalise chained `Where*` invocations
        /// against the original root regardless of the chain depth.
        /// </summary>
        protected override EquipmentEnchantOptionBinderCollection? GetRootSource() => this;

        /// <summary>
        /// Routes the navigation-time disposed guard to the owning lifecycle
        /// check so reference navigation on a disposed collection throws
        /// <see cref="ObjectDisposedException"/>.
        /// </summary>
        protected override void ThrowIfDisposedForNavigation() => ThrowIfDisposed();

        // Wiring events carrying the owning binder contract (IEquipmentEnchantOptionBinder).
        // Public so a fake IEquipmentEnchantOptionBinderCollection can raise them; the ListHandler
        // subscribes to build/bind child handlers (which need the owning Subscribe),
        // then re-exposes its own public Action<IActionableEquipmentEnchantOptionBinder> events.
        /// <summary>Fires when a new element binder is added.</summary>
        public event Action<IEquipmentEnchantOptionBinder>? ItemAdded;

        /// <summary>Fires when an element binder is removed. The binder is
        /// Disposed after the event handlers return.</summary>
        public event Action<IEquipmentEnchantOptionBinder>? ItemRemoved;

        public EquipmentEnchantOptionBinderCollection(
            Gs2Domain gs2,
            IGameSession session)
        {
            _gs2 = gs2;
            _session = session;
        }

        public void Invalidate()
        {
            ThrowIfDisposed();

        }

        /// <summary>
        /// Sorts the private <c>_binders</c> list into the configured order.
        /// Called at the tail of every reconcile so consumers see stable
        /// ordering across mounts and subscription callbacks. The comparer
        /// supplies the primary key and Id tie-break; LINQ <c>OrderBy</c>
        /// preserves input order when both return 0. An authoring-declared
        /// sort key changes the comparer slot, not this shared sort body.
        /// </summary>
        private void SortBinders()
        {
            var sorted = _binders.OrderBy(b => b, _effectiveComparer).ToList();
            _binders.Clear();
            _binders.AddRange(sorted);
            RaiseSourceChanged();
        }

        /// <summary>
        /// Registers a filter view so subsequent source mutations refresh
        /// the view's cache. The view is held by weak reference; dead
        /// entries are pruned during <see cref="RaiseSourceChanged"/>.
        /// </summary>
        internal void RegisterDerivedView(ReadOnlyEquipmentEnchantOptionBinderCollection view)
        {
            _derivedViews.Add(new WeakReference<ReadOnlyEquipmentEnchantOptionBinderCollection>(view));
        }

        /// <summary>
        /// Fans the source-changed signal out to every live filter view
        /// (and prunes dead weak references in the same pass). Invoked at
        /// the tail of every reconcile via <see cref="SortBinders"/>.
        /// </summary>
        private void RaiseSourceChanged()
        {
            for (int i = _derivedViews.Count - 1; i >= 0; i--)
            {
                if (_derivedViews[i].TryGetTarget(out var view))
                {
                    view.RefreshCache();
                }
                else
                {
                    _derivedViews.RemoveAt(i);
                }
            }
        }

        public void Dispose()
        {
            if (_disposed) return;
            _disposed = true;
            for (int i = _derivedViews.Count - 1; i >= 0; i--)
            {
                if (_derivedViews[i].TryGetTarget(out var view)) view.MarkDead();
            }
            _derivedViews.Clear();
            foreach (var u in _unsubscribers) u?.Invoke();
            _unsubscribers.Clear();
            foreach (var b in _binders) b.Dispose();
            _binders.Clear();
            _bindersByRowKey.Clear();
            GC.SuppressFinalize(this);
        }

        private void ThrowIfDisposed()
        {
            if (_disposed)
            {
                throw new ObjectDisposedException(GetType().Name);
            }
        }

        /// <summary>One-shot Create + MountFromOverlay (no subscription).</summary>
        public static async Task<EquipmentEnchantOptionBinderCollection> CreateFromOverlayAsync(
            Gs2Domain gs2,
            IGameSession session,
            CancellationToken cancellationToken = default)
        {
            var coll = new EquipmentEnchantOptionBinderCollection(gs2, session);
            await coll.MountFromOverlayAsync(cancellationToken);
            return coll;
        }

        public async Task MountFromOverlayAsync(CancellationToken cancellationToken = default)
        {
            ThrowIfDisposed();
            cancellationToken.ThrowIfCancellationRequested();
            var entries = EquipmentEnchantOptionOverlayLoader.Active?.GetAll();
            if (entries == null) return;
            var seen = new HashSet<string>();
            foreach (var entry in entries)
            {
                if (string.IsNullOrEmpty(entry.RawInstanceId)) continue;
                var rowKey = entry.RawInstanceId;
                var id = new EquipmentEnchantOptionId(entry.RawInstanceId);
                if (EqualityComparer<EquipmentEnchantOptionId>.Default.Equals(id, default)) continue;
                seen.Add(rowKey);
                if (_bindersByRowKey.TryGetValue(rowKey, out var existing))
                {
                    entry.ApplyTo(existing.MutableModel);
                    continue;
                }
                var model = EquipmentEnchantOptionBinder.CreateModel(id);
                entry.ApplyTo(model);
                var binder = new EquipmentEnchantOptionBinder(model, _gs2, _session);
                await binder.MountAsync(cancellationToken);
                if (_disposed)
                {
                    binder.Dispose();
                    return;
                }
                if (_bindersByRowKey.TryGetValue(rowKey, out var raced))
                {
                    entry.ApplyTo(raced.MutableModel);
                    binder.Dispose();
                    continue;
                }
                _binders.Add(binder);
                _bindersByRowKey[rowKey] = binder;
                ItemAdded?.Invoke(binder);
            }
            if (_bindersByRowKey.Count > seen.Count)
            {
                var toRemove = new List<string>();
                foreach (var kv in _bindersByRowKey)
                {
                    if (!seen.Contains(kv.Key)) toRemove.Add(kv.Key);
                }
                // Detach every stale binder before callbacks; a callback may dispose this Collection.
                var pendingRemovals = new List<Action>();
                foreach (var staleRowKey in toRemove)
                {
                    if (!_bindersByRowKey.TryGetValue(staleRowKey, out var binder)) continue;
                    _bindersByRowKey.Remove(staleRowKey);
                    _binders.Remove(binder);
                    var detachedBinder = binder;
                    // Notify while the binder is live; the Collection retains disposal ownership.
                    pendingRemovals.Add(() =>
                    {
                        try
                        {
                            if (!_disposed) ItemRemoved?.Invoke(detachedBinder);
                        }
                        finally
                        {
                            detachedBinder.Dispose();
                        }
                    });
                }
                Exception? removalError = null;
                foreach (var remove in pendingRemovals)
                {
                    try { remove(); }
                    catch (Exception ex) { removalError ??= ex; }
                }
                if (removalError != null) throw removalError;
            }
            if (!_disposed) SortBinders();
            _mounted = true;
        }

        internal sealed class EquipmentEnchantOptionBinderComparer : IComparer<IReadOnlyEquipmentEnchantOptionBinder>
        {
            public static readonly EquipmentEnchantOptionBinderComparer Default = new EquipmentEnchantOptionBinderComparer();
            public int Compare(IReadOnlyEquipmentEnchantOptionBinder? a, IReadOnlyEquipmentEnchantOptionBinder? b)
            {
                if (a is null && b is null) return 0;
                if (a is null) return -1;
                if (b is null) return 1;
                return Comparer<EquipmentEnchantOptionId>.Default.Compare(a.Id, b.Id);
            }
        }

        internal sealed class EquipmentEnchantOptionBinderIdTieBreakComparer : IComparer<IReadOnlyEquipmentEnchantOptionBinder>
        {
            private readonly IComparer<IReadOnlyEquipmentEnchantOptionBinder> _inner;
            public EquipmentEnchantOptionBinderIdTieBreakComparer(IComparer<IReadOnlyEquipmentEnchantOptionBinder> inner) { _inner = inner; }
            public int Compare(IReadOnlyEquipmentEnchantOptionBinder? a, IReadOnlyEquipmentEnchantOptionBinder? b)
            {
                if (a is null && b is null) return 0;
                if (a is null) return -1;
                if (b is null) return 1;
                var c = _inner.Compare(a, b);
                if (c != 0) return c;
                return Comparer<EquipmentEnchantOptionId>.Default.Compare(a.Id, b.Id);
            }
        }
    }
}
