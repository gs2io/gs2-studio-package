// <auto-generated>
//     This code was generated by CSharpBinderGeneratorService.
//     Do not modify this file directly.
// </auto-generated>

#nullable enable

using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;

using Gs2.Unity.Core;
using Gs2.Unity.Util;
using GS2Studio.Generated.UsageLimit;
using Gs2.Unity.Core.Model;
using Gs2.Unity.Gs2Exchange.Model;
using Gs2.Unity.Gs2Limit.Model;
using Gs2Bind.Gs2Exchange;
using Gs2Bind.Gs2Limit;

namespace GS2Studio.Generated.UsageLimitCounter
{
    /// <summary>
    /// Public read contract for the UsageLimitCounter binder collection: the
    /// stable <see cref="IReadOnlyList{T}"/> over the actionable (non-owning)
    /// element binders. Access is index/enumeration based — the domain id is
    /// not unique per row (multiple backing rows may fan out to instances
    /// sharing one id), so id lookup is a caller-side LINQ concern.
    /// Substitutable seam for code that only enumerates binders.
    /// </summary>
    public interface IReadOnlyUsageLimitCounterBinderCollection : IReadOnlyList<IActionableUsageLimitCounterBinder>
    {
        IReadOnlyUsageLimitCounterBinderCollection WhereLimit(UsageLimitId limit);
    }

    /// <summary>
    /// Owning contract for the UsageLimitCounter binder collection: adds the
    /// lifecycle + mount/subscribe surface and the owning-binder wiring events
    /// over <see cref="IReadOnlyUsageLimitCounterBinderCollection"/>. Returned by the binder
    /// factory; a fake implementation substitutes the whole collection.
    /// </summary>
    public interface IUsageLimitCounterBinderCollection : IReadOnlyUsageLimitCounterBinderCollection, IDisposable
    {
        IComparer<IReadOnlyUsageLimitCounterBinder> Comparer { get; set; }
        event Action<IUsageLimitCounterBinder> ItemAdded;
        event Action<IUsageLimitCounterBinder> ItemRemoved;
        void Invalidate();
        Task MountFromExchangeLimitResetMasterDataAsync(CancellationToken cancellationToken = default);
        void SubscribeFromExchangeLimitResetMasterData(Action? onChange = null, Action<Exception>? onError = null);
        Task MountFromExchangeLimitFreeUseMasterDataAsync(CancellationToken cancellationToken = default);
        void SubscribeFromExchangeLimitFreeUseMasterData(Action? onChange = null, Action<Exception>? onError = null);
        Task MountFromExchangeLimitAdUseMasterDataAsync(CancellationToken cancellationToken = default);
        void SubscribeFromExchangeLimitAdUseMasterData(Action? onChange = null, Action<Exception>? onError = null);
        Task MountFromLimitLimitUserDataAsync(CancellationToken cancellationToken = default);
        void SubscribeFromLimitLimitUserData(Action? onChange = null, Action<Exception>? onError = null);
    }

    /// <summary>
    /// Lifecycle-free base for <see cref="UsageLimitCounterBinderCollection"/>.
    /// Exposes the <see cref="IReadOnlyList{T}"/> contract over
    /// <see cref="IActionableUsageLimitCounterBinder"/> and owns the
    /// <c>Where{RefProp}</c> reference-navigation filter helpers. Filter view
    /// instances reach the owning root through the internal ctor + the
    /// <c>RegisterDerivedView</c> hook. Never exposes <c>Dispose</c>,
    /// <c>Mount*</c>, or <c>Subscribe*</c>.
    /// </summary>
    public class ReadOnlyUsageLimitCounterBinderCollection : IReadOnlyUsageLimitCounterBinderCollection
    {
        // Filter-view backing state. The owning derived class leaves these
        // null and overrides every observation member, so the `_cache == null`
        // / `_dead` branches only kick in for filter views and the static
        // `Empty` instance.
        private readonly UsageLimitCounterBinderCollection? _rootSource;
        private readonly Predicate<IReadOnlyUsageLimitCounterBinder>? _predicate;
        private readonly List<IActionableUsageLimitCounterBinder>? _cache;
        private bool _dead;

        // Shared sentinel list backing the `_dead` / `Empty` enumeration
        // paths. Reused so a dead view does not allocate per access.
        private static readonly List<IActionableUsageLimitCounterBinder> EmptyList = new List<IActionableUsageLimitCounterBinder>(0);

        /// <summary>
        /// Sentinel non-owning empty collection. Returned by <c>Where*</c>
        /// fast-exit paths (disposed view, dead view, default-id, no root).
        /// </summary>
        internal static ReadOnlyUsageLimitCounterBinderCollection Empty { get; } = new ReadOnlyUsageLimitCounterBinderCollection();

        /// <summary>
        /// Parameterless ctor for the owning derived class and the
        /// <see cref="Empty"/> sentinel. The view state stays null so the
        /// fallback `_cache == null` branch keeps the sentinel safe.
        /// </summary>
        protected ReadOnlyUsageLimitCounterBinderCollection() { }

        /// <summary>
        /// Filter-view ctor. Registers the view with the owning root so
        /// subsequent source mutations refresh this view's cache.
        /// </summary>
        internal ReadOnlyUsageLimitCounterBinderCollection(
            UsageLimitCounterBinderCollection rootSource,
            Predicate<IReadOnlyUsageLimitCounterBinder> predicate)
        {
            _rootSource = rootSource;
            _predicate = predicate;
            _cache = new List<IActionableUsageLimitCounterBinder>();
            rootSource.RegisterDerivedView(this);
            RefreshCache();
        }

        /// <summary>Element count. Derived owning collection overrides.</summary>
        public virtual int Count => (_dead || _cache == null) ? 0 : _cache.Count;

        /// <summary>Indexer. Derived owning collection overrides.</summary>
        public virtual IActionableUsageLimitCounterBinder this[int index]
            => (_dead || _cache == null) ? EmptyList[index] : _cache[index];

        /// <summary>Enumerator. Derived owning collection overrides.</summary>
        public virtual IEnumerator<IActionableUsageLimitCounterBinder> GetEnumerator()
            => (_dead || _cache == null) ? EmptyList.GetEnumerator() : _cache.GetEnumerator();

        IEnumerator IEnumerable.GetEnumerator() => GetEnumerator();

        /// <summary>
        /// Returns the owning root collection backing this view. Derived
        /// owning collection returns itself; filter views return the source
        /// they were constructed against.
        /// </summary>
        protected virtual UsageLimitCounterBinderCollection? GetRootSource() => _rootSource;

        /// <summary>
        /// Disposed-guard hook used by reference-navigation methods. Base
        /// implementation is a no-op so filter views and the <see cref="Empty"/>
        /// sentinel do not need to know about lifecycle; the owning derived
        /// class overrides this to call <c>ThrowIfDisposed</c>.
        /// </summary>
        protected virtual void ThrowIfDisposedForNavigation() { }

        /// <summary>
        /// Rebuilds this view's cache from the root collection's currently
        /// owned binders. No-op when the view is dead, lacks a root source,
        /// or is the parameterless <see cref="Empty"/> sentinel.
        /// </summary>
        internal void RefreshCache()
        {
            if (_dead || _rootSource == null || _cache == null || _predicate == null) return;
            _cache.Clear();
            foreach (var b in _rootSource)
            {
                if (_predicate(b)) _cache.Add(b);
            }
        }

        /// <summary>
        /// Marks the view dead. Subsequent observation returns the empty
        /// sentinel and `Where*` short-circuits to <see cref="Empty"/>.
        /// </summary>
        internal void MarkDead()
        {
            _dead = true;
            _cache?.Clear();
        }


        #region Reference navigation
        public IReadOnlyUsageLimitCounterBinderCollection WhereLimit(UsageLimitId limit)
        {
            ThrowIfDisposedForNavigation();
            if (_dead) return Empty;
            if (EqualityComparer<UsageLimitId>.Default.Equals(limit, default)) return Empty;
            var root = GetRootSource();
            if (root == null) return Empty;
            var existing = _predicate;
            Predicate<IReadOnlyUsageLimitCounterBinder> composed = existing == null
                ? (Predicate<IReadOnlyUsageLimitCounterBinder>)(b => MatchLimit(b, limit))
                : b => existing(b) && MatchLimit(b, limit);
            return new ReadOnlyUsageLimitCounterBinderCollection(root, composed);
        }
        private static bool MatchLimit(IReadOnlyUsageLimitCounterBinder b, UsageLimitId limit)
            => EqualityComparer<UsageLimitId>.Default.Equals(b.Limit, limit);
        #endregion

    }

    /// <summary>
    /// Collection binder for UsageLimitCounter.
    /// Wraps a list of UsageLimitCounterBinder elements and reflects list-level
    /// changes (additions / removals) and per-element changes from the GS2
    /// data sources, using identity-based reconciliation so element binder
    /// instances stay stable across list updates.
    /// </summary>
    public sealed class UsageLimitCounterBinderCollection : ReadOnlyUsageLimitCounterBinderCollection, IUsageLimitCounterBinderCollection
    {
        private readonly Gs2Domain _gs2;
        private readonly IGameSession _session;
        private readonly string _limit;
        // Owning binders are internal: consumers read the collection as the
        // non-owning IReadOnlyList<IActionableUsageLimitCounterBinder> contract; only the
        // collection (and the wiring ItemAdded/ItemRemoved events) touch the
        // owning UsageLimitCounterBinder so lifecycle (Subscribe/Dispose) never leaks.
        private readonly List<UsageLimitCounterBinder> _binders = new List<UsageLimitCounterBinder>();
        // Reconcile index keyed by the per-item ROW KEY (backing row identity),
        // not by Model.Id: the domain id may legitimately repeat across rows
        // (fan-out), while the row key is unique within one collection.
        private readonly Dictionary<string, UsageLimitCounterBinder> _bindersByRowKey = new Dictionary<string, UsageLimitCounterBinder>();
        private readonly List<Action> _unsubscribers = new List<Action>();
        // Weakly-referenced filter views so view-only consumers can be GC'd
        // without forcing the owning collection to hold them alive. Dead
        // entries are pruned during `RaiseSourceChanged`.
        private readonly List<WeakReference<ReadOnlyUsageLimitCounterBinderCollection>> _derivedViews = new List<WeakReference<ReadOnlyUsageLimitCounterBinderCollection>>();
        private bool _disposed;
        private bool _mounted;
        private bool _subscriptionActive;
        private Action? _onChange;

        // Sort comparers — `_configuredComparer` is what consumers observe via
        // the `Comparer` getter; `_effectiveComparer` wraps it with an Id
        // tie-break for distinct IDs. Same-ID rows can still compare equal;
        // SortBinders uses stable OrderBy to preserve their input order.
        private IComparer<IReadOnlyUsageLimitCounterBinder> _configuredComparer = UsageLimitCounterBinderComparer.Default;
        private IComparer<IReadOnlyUsageLimitCounterBinder> _effectiveComparer =
            new UsageLimitCounterBinderIdTieBreakComparer(UsageLimitCounterBinderComparer.Default);

        /// <summary>
        /// User-facing comparer driving the binder sort order. Reading returns
        /// the value last assigned (no wrapper leakage). Setting installs an
        /// Id-tie-break wrapper internally so distinct Model.Id values have a
        /// deterministic fallback when the supplied comparer returns 0. Rows
        /// with the same Id remain equal and rely on SortBinders' stable
        /// OrderBy. Typed over the non-owning IReadOnlyUsageLimitCounterBinder so
        /// it never exposes the owning binder.
        /// </summary>
        public IComparer<IReadOnlyUsageLimitCounterBinder> Comparer
        {
            get => _configuredComparer;
            set
            {
                var c = value ?? throw new ArgumentNullException(nameof(value));
                _configuredComparer = c;
                _effectiveComparer = new UsageLimitCounterBinderIdTieBreakComparer(c);
                SortBinders();
                _onChange?.Invoke();
            }
        }

        /// <summary>Element count over the owning binder list.</summary>
        public override int Count => _binders.Count;

        /// <summary>
        /// Indexer over the owning binder list. Return type is the non-owning
        /// IActionableUsageLimitCounterBinder contract — the public surface never exposes
        /// the owning binder.
        /// </summary>
        public override IActionableUsageLimitCounterBinder this[int index] => _binders[index];

        /// <summary>
        /// Enumerator over the owning binder list, yielding the non-owning
        /// IActionableUsageLimitCounterBinder contract.
        /// </summary>
        public override IEnumerator<IActionableUsageLimitCounterBinder> GetEnumerator() => _binders.GetEnumerator();

        /// <summary>
        /// Returns the owning root collection — itself. Filter views call
        /// this on their source to normalise chained `Where*` invocations
        /// against the original root regardless of the chain depth.
        /// </summary>
        protected override UsageLimitCounterBinderCollection? GetRootSource() => this;

        /// <summary>
        /// Routes the navigation-time disposed guard to the owning lifecycle
        /// check so reference navigation on a disposed collection throws
        /// <see cref="ObjectDisposedException"/>.
        /// </summary>
        protected override void ThrowIfDisposedForNavigation() => ThrowIfDisposed();

        // Wiring events carrying the owning binder contract (IUsageLimitCounterBinder).
        // Public so a fake IUsageLimitCounterBinderCollection can raise them; the ListHandler
        // subscribes to build/bind child handlers (which need the owning Subscribe),
        // then re-exposes its own public Action<IActionableUsageLimitCounterBinder> events.
        /// <summary>Fires when a new element binder is added.</summary>
        public event Action<IUsageLimitCounterBinder>? ItemAdded;

        /// <summary>Fires when an element binder is removed. The binder is
        /// Disposed after the event handlers return.</summary>
        public event Action<IUsageLimitCounterBinder>? ItemRemoved;

        public UsageLimitCounterBinderCollection(
            Gs2Domain gs2,
            IGameSession session,
            string limit)
        {
            _gs2 = gs2;
            _session = session;
            _limit = limit;
        }

        public void Invalidate()
        {
            ThrowIfDisposed();
            new Gs2Bind.Gs2Exchange.RateModelArrayLoader("LimitReset").Invalidate(_gs2, _session);
            new Gs2Bind.Gs2Exchange.RateModelArrayLoader("LimitFreeUse").Invalidate(_gs2, _session);
            new Gs2Bind.Gs2Exchange.RateModelArrayLoader("LimitAdUse").Invalidate(_gs2, _session);
            new Gs2Bind.Gs2Limit.CounterArrayLoader("Limit").Invalidate(_gs2, _session);
        }

        /// <summary>
        /// Sorts the private <c>_binders</c> list into the configured order.
        /// Called at the tail of every reconcile so consumers see stable
        /// ordering across mounts and subscription callbacks. The comparer
        /// supplies the primary key and Id tie-break; LINQ <c>OrderBy</c>
        /// preserves input order when both return 0. An authoring-declared
        /// sort key changes the comparer slot, not this shared sort body.
        /// </summary>
        private void SortBinders()
        {
            var sorted = _binders.OrderBy(b => b, _effectiveComparer).ToList();
            _binders.Clear();
            _binders.AddRange(sorted);
            RaiseSourceChanged();
        }

        /// <summary>
        /// Registers a filter view so subsequent source mutations refresh
        /// the view's cache. The view is held by weak reference; dead
        /// entries are pruned during <see cref="RaiseSourceChanged"/>.
        /// </summary>
        internal void RegisterDerivedView(ReadOnlyUsageLimitCounterBinderCollection view)
        {
            _derivedViews.Add(new WeakReference<ReadOnlyUsageLimitCounterBinderCollection>(view));
        }

        /// <summary>
        /// Fans the source-changed signal out to every live filter view
        /// (and prunes dead weak references in the same pass). Invoked at
        /// the tail of every reconcile via <see cref="SortBinders"/>.
        /// </summary>
        private void RaiseSourceChanged()
        {
            for (int i = _derivedViews.Count - 1; i >= 0; i--)
            {
                if (_derivedViews[i].TryGetTarget(out var view))
                {
                    view.RefreshCache();
                }
                else
                {
                    _derivedViews.RemoveAt(i);
                }
            }
        }

        public void Dispose()
        {
            if (_disposed) return;
            _disposed = true;
            for (int i = _derivedViews.Count - 1; i >= 0; i--)
            {
                if (_derivedViews[i].TryGetTarget(out var view)) view.MarkDead();
            }
            _derivedViews.Clear();
            foreach (var u in _unsubscribers) u?.Invoke();
            _unsubscribers.Clear();
            foreach (var b in _binders) b.Dispose();
            _binders.Clear();
            _bindersByRowKey.Clear();
            GC.SuppressFinalize(this);
        }

        private void ThrowIfDisposed()
        {
            if (_disposed)
            {
                throw new ObjectDisposedException(GetType().Name);
            }
        }

        /// <summary>One-shot Create + MountFromExchangeLimitResetMasterDataAsync (no subscription).</summary>
        public static async Task<UsageLimitCounterBinderCollection> CreateFromExchangeLimitResetMasterDataAsync(
            Gs2Domain gs2,
            IGameSession session,
            string limit,
            CancellationToken cancellationToken = default)
        {
            var coll = new UsageLimitCounterBinderCollection(gs2, session, limit);
            await coll.MountFromExchangeLimitResetMasterDataAsync(cancellationToken);
            return coll;
        }

        public async Task MountFromExchangeLimitResetMasterDataAsync(CancellationToken cancellationToken = default)
        {
            ThrowIfDisposed();
            cancellationToken.ThrowIfCancellationRequested();
            var arrayLoader = new Gs2Bind.Gs2Exchange.RateModelArrayLoader("LimitReset");
            var items = await arrayLoader.Load(_gs2, _session);
            cancellationToken.ThrowIfCancellationRequested();
            await ReconcileFromExchangeLimitResetMasterItems(items, attachChildSubscribe: false, cancellationToken);
            _mounted = true;
        }

        public void SubscribeFromExchangeLimitResetMasterData(Action? onChange = null, Action<Exception>? onError = null)
        {
            ThrowIfDisposed();
            if (_subscriptionActive) throw new InvalidOperationException("Already subscribed");
            _subscriptionActive = true;
            // Consumer-facing notification, wrapped once so a throwing consumer
            // callback routes to onError (or Debug) instead of escaping the
            // loader's async-void chain.
            Action notify = () =>
            {
                try { onChange?.Invoke(); }
                catch (Exception ex) { if (onError != null) onError(ex); else UnityEngine.Debug.LogException(ex); }
            };
            _onChange = notify;
            foreach (var b in _binders) b.Subscribe(notify);
            var arrayLoader = new Gs2Bind.Gs2Exchange.RateModelArrayLoader("LimitReset");
            _unsubscribers.Add(arrayLoader.Subscribe(
                _gs2,
                _session,
                async (_, _, items) =>
                {
                    if (_disposed) return;
                    try
                    {
                        await ReconcileFromExchangeLimitResetMasterItems(items, attachChildSubscribe: true, CancellationToken.None);
                    }
                    catch (Exception ex)
                    {
                        if (onError != null) onError(ex); else UnityEngine.Debug.LogException(ex);
                    }
                },
                notify
            ));
        }

        private async Task ReconcileFromExchangeLimitResetMasterItems(IList<Gs2.Unity.Gs2Exchange.Model.EzRateModel> items, bool attachChildSubscribe, CancellationToken cancellationToken)
        {
            var seen = new HashSet<string>();
            foreach (var item in items)
            {
                if (_disposed) return;
                var rowKey = ExtractExchangeLimitResetMasterRowKey(item);
                if (rowKey == null) continue;
                seen.Add(rowKey);
                if (_bindersByRowKey.TryGetValue(rowKey, out var existing))
                {
                    ApplyExchangeLimitResetMasterItemTo(existing.MutableModel, item);
                }
                else
                {
                    var binder = await BuildBinderFromExchangeLimitResetMasterItem(item, cancellationToken);
                    if (_disposed)
                    {
                        binder.Dispose();
                        return;
                    }
                    if (_bindersByRowKey.TryGetValue(rowKey, out var raced))
                    {
                        ApplyExchangeLimitResetMasterItemTo(raced.MutableModel, item);
                        binder.Dispose();
                        continue;
                    }
                    if (attachChildSubscribe) binder.Subscribe(_onChange);
                    _bindersByRowKey[rowKey] = binder;
                    _binders.Add(binder);
                    ItemAdded?.Invoke(binder);
                }
            }
            if (_bindersByRowKey.Count > seen.Count)
            {
                var toRemove = new List<string>();
                foreach (var kv in _bindersByRowKey)
                {
                    if (!seen.Contains(kv.Key)) toRemove.Add(kv.Key);
                }
                // Detach every stale binder before callbacks; a callback may dispose this Collection.
                var pendingRemovals = new List<Action>();
                foreach (var rowKey in toRemove)
                {
                    if (!_bindersByRowKey.TryGetValue(rowKey, out var binder)) continue;
                    _bindersByRowKey.Remove(rowKey);
                    _binders.Remove(binder);
                    var detachedBinder = binder;
                    // Notify while the binder is live; the Collection retains disposal ownership.
                    pendingRemovals.Add(() =>
                    {
                        try
                        {
                            if (!_disposed) ItemRemoved?.Invoke(detachedBinder);
                        }
                        finally
                        {
                            detachedBinder.Dispose();
                        }
                    });
                }
                Exception? removalError = null;
                foreach (var remove in pendingRemovals)
                {
                    try { remove(); }
                    catch (Exception ex) { removalError ??= ex; }
                }
                if (removalError != null) throw removalError;
            }
            if (!_disposed) SortBinders();
        }

        private async Task<UsageLimitCounterBinder> BuildBinderFromExchangeLimitResetMasterItem(Gs2.Unity.Gs2Exchange.Model.EzRateModel item, CancellationToken cancellationToken)
        {
            var model = UsageLimitCounterBinder.CreateModel((string.IsNullOrEmpty(item.Name) ? default(UsageLimitCounterId) : new UsageLimitCounterId(item.Name)), new UsageLimitId(_limit));
            ApplyExchangeLimitResetMasterItemTo(model, item);
            var binder = new UsageLimitCounterBinder(model, _gs2, _session);
            await binder.MountAsync(cancellationToken);
            return binder;
        }

        private static void ApplyExchangeLimitResetMasterItemTo(MutableUsageLimitCounter model, Gs2.Unity.Gs2Exchange.Model.EzRateModel item)
        {
            // This loader carries no master-item field assignments; reconcile manages
            // membership only (per-element field changes are tracked by each element
            // binder's own Subscribe).
            _ = item;
            _ = model;
        }

        private UsageLimitCounterId ExtractExchangeLimitResetMasterIdentity(Gs2.Unity.Gs2Exchange.Model.EzRateModel item)
        {
            return (string.IsNullOrEmpty(item.Name) ? default(UsageLimitCounterId) : new UsageLimitCounterId(item.Name));
        }

        private string? ExtractExchangeLimitResetMasterRowKey(Gs2.Unity.Gs2Exchange.Model.EzRateModel item)
        {
            var id = ExtractExchangeLimitResetMasterIdentity(item);
            if (EqualityComparer<UsageLimitCounterId>.Default.Equals(id, default)) return null;
            if (string.IsNullOrEmpty(item.Name)) return null;
            return $"{item.Name}";
        }
        /// <summary>One-shot Create + MountFromExchangeLimitFreeUseMasterDataAsync (no subscription).</summary>
        public static async Task<UsageLimitCounterBinderCollection> CreateFromExchangeLimitFreeUseMasterDataAsync(
            Gs2Domain gs2,
            IGameSession session,
            string limit,
            CancellationToken cancellationToken = default)
        {
            var coll = new UsageLimitCounterBinderCollection(gs2, session, limit);
            await coll.MountFromExchangeLimitFreeUseMasterDataAsync(cancellationToken);
            return coll;
        }

        public async Task MountFromExchangeLimitFreeUseMasterDataAsync(CancellationToken cancellationToken = default)
        {
            ThrowIfDisposed();
            cancellationToken.ThrowIfCancellationRequested();
            var arrayLoader = new Gs2Bind.Gs2Exchange.RateModelArrayLoader("LimitFreeUse");
            var items = await arrayLoader.Load(_gs2, _session);
            cancellationToken.ThrowIfCancellationRequested();
            await ReconcileFromExchangeLimitFreeUseMasterItems(items, attachChildSubscribe: false, cancellationToken);
            _mounted = true;
        }

        public void SubscribeFromExchangeLimitFreeUseMasterData(Action? onChange = null, Action<Exception>? onError = null)
        {
            ThrowIfDisposed();
            if (_subscriptionActive) throw new InvalidOperationException("Already subscribed");
            _subscriptionActive = true;
            // Consumer-facing notification, wrapped once so a throwing consumer
            // callback routes to onError (or Debug) instead of escaping the
            // loader's async-void chain.
            Action notify = () =>
            {
                try { onChange?.Invoke(); }
                catch (Exception ex) { if (onError != null) onError(ex); else UnityEngine.Debug.LogException(ex); }
            };
            _onChange = notify;
            foreach (var b in _binders) b.Subscribe(notify);
            var arrayLoader = new Gs2Bind.Gs2Exchange.RateModelArrayLoader("LimitFreeUse");
            _unsubscribers.Add(arrayLoader.Subscribe(
                _gs2,
                _session,
                async (_, _, items) =>
                {
                    if (_disposed) return;
                    try
                    {
                        await ReconcileFromExchangeLimitFreeUseMasterItems(items, attachChildSubscribe: true, CancellationToken.None);
                    }
                    catch (Exception ex)
                    {
                        if (onError != null) onError(ex); else UnityEngine.Debug.LogException(ex);
                    }
                },
                notify
            ));
        }

        private async Task ReconcileFromExchangeLimitFreeUseMasterItems(IList<Gs2.Unity.Gs2Exchange.Model.EzRateModel> items, bool attachChildSubscribe, CancellationToken cancellationToken)
        {
            var seen = new HashSet<string>();
            foreach (var item in items)
            {
                if (_disposed) return;
                var rowKey = ExtractExchangeLimitFreeUseMasterRowKey(item);
                if (rowKey == null) continue;
                seen.Add(rowKey);
                if (_bindersByRowKey.TryGetValue(rowKey, out var existing))
                {
                    ApplyExchangeLimitFreeUseMasterItemTo(existing.MutableModel, item);
                }
                else
                {
                    var binder = await BuildBinderFromExchangeLimitFreeUseMasterItem(item, cancellationToken);
                    if (_disposed)
                    {
                        binder.Dispose();
                        return;
                    }
                    if (_bindersByRowKey.TryGetValue(rowKey, out var raced))
                    {
                        ApplyExchangeLimitFreeUseMasterItemTo(raced.MutableModel, item);
                        binder.Dispose();
                        continue;
                    }
                    if (attachChildSubscribe) binder.Subscribe(_onChange);
                    _bindersByRowKey[rowKey] = binder;
                    _binders.Add(binder);
                    ItemAdded?.Invoke(binder);
                }
            }
            if (_bindersByRowKey.Count > seen.Count)
            {
                var toRemove = new List<string>();
                foreach (var kv in _bindersByRowKey)
                {
                    if (!seen.Contains(kv.Key)) toRemove.Add(kv.Key);
                }
                // Detach every stale binder before callbacks; a callback may dispose this Collection.
                var pendingRemovals = new List<Action>();
                foreach (var rowKey in toRemove)
                {
                    if (!_bindersByRowKey.TryGetValue(rowKey, out var binder)) continue;
                    _bindersByRowKey.Remove(rowKey);
                    _binders.Remove(binder);
                    var detachedBinder = binder;
                    // Notify while the binder is live; the Collection retains disposal ownership.
                    pendingRemovals.Add(() =>
                    {
                        try
                        {
                            if (!_disposed) ItemRemoved?.Invoke(detachedBinder);
                        }
                        finally
                        {
                            detachedBinder.Dispose();
                        }
                    });
                }
                Exception? removalError = null;
                foreach (var remove in pendingRemovals)
                {
                    try { remove(); }
                    catch (Exception ex) { removalError ??= ex; }
                }
                if (removalError != null) throw removalError;
            }
            if (!_disposed) SortBinders();
        }

        private async Task<UsageLimitCounterBinder> BuildBinderFromExchangeLimitFreeUseMasterItem(Gs2.Unity.Gs2Exchange.Model.EzRateModel item, CancellationToken cancellationToken)
        {
            var model = UsageLimitCounterBinder.CreateModel((string.IsNullOrEmpty(item.Name) ? default(UsageLimitCounterId) : new UsageLimitCounterId(item.Name)), new UsageLimitId(_limit));
            ApplyExchangeLimitFreeUseMasterItemTo(model, item);
            var binder = new UsageLimitCounterBinder(model, _gs2, _session);
            await binder.MountAsync(cancellationToken);
            return binder;
        }

        private static void ApplyExchangeLimitFreeUseMasterItemTo(MutableUsageLimitCounter model, Gs2.Unity.Gs2Exchange.Model.EzRateModel item)
        {
            // This loader carries no master-item field assignments; reconcile manages
            // membership only (per-element field changes are tracked by each element
            // binder's own Subscribe).
            _ = item;
            _ = model;
        }

        private UsageLimitCounterId ExtractExchangeLimitFreeUseMasterIdentity(Gs2.Unity.Gs2Exchange.Model.EzRateModel item)
        {
            return (string.IsNullOrEmpty(item.Name) ? default(UsageLimitCounterId) : new UsageLimitCounterId(item.Name));
        }

        private string? ExtractExchangeLimitFreeUseMasterRowKey(Gs2.Unity.Gs2Exchange.Model.EzRateModel item)
        {
            var id = ExtractExchangeLimitFreeUseMasterIdentity(item);
            if (EqualityComparer<UsageLimitCounterId>.Default.Equals(id, default)) return null;
            if (string.IsNullOrEmpty(item.Name)) return null;
            return $"{item.Name}";
        }
        /// <summary>One-shot Create + MountFromExchangeLimitAdUseMasterDataAsync (no subscription).</summary>
        public static async Task<UsageLimitCounterBinderCollection> CreateFromExchangeLimitAdUseMasterDataAsync(
            Gs2Domain gs2,
            IGameSession session,
            string limit,
            CancellationToken cancellationToken = default)
        {
            var coll = new UsageLimitCounterBinderCollection(gs2, session, limit);
            await coll.MountFromExchangeLimitAdUseMasterDataAsync(cancellationToken);
            return coll;
        }

        public async Task MountFromExchangeLimitAdUseMasterDataAsync(CancellationToken cancellationToken = default)
        {
            ThrowIfDisposed();
            cancellationToken.ThrowIfCancellationRequested();
            var arrayLoader = new Gs2Bind.Gs2Exchange.RateModelArrayLoader("LimitAdUse");
            var items = await arrayLoader.Load(_gs2, _session);
            cancellationToken.ThrowIfCancellationRequested();
            await ReconcileFromExchangeLimitAdUseMasterItems(items, attachChildSubscribe: false, cancellationToken);
            _mounted = true;
        }

        public void SubscribeFromExchangeLimitAdUseMasterData(Action? onChange = null, Action<Exception>? onError = null)
        {
            ThrowIfDisposed();
            if (_subscriptionActive) throw new InvalidOperationException("Already subscribed");
            _subscriptionActive = true;
            // Consumer-facing notification, wrapped once so a throwing consumer
            // callback routes to onError (or Debug) instead of escaping the
            // loader's async-void chain.
            Action notify = () =>
            {
                try { onChange?.Invoke(); }
                catch (Exception ex) { if (onError != null) onError(ex); else UnityEngine.Debug.LogException(ex); }
            };
            _onChange = notify;
            foreach (var b in _binders) b.Subscribe(notify);
            var arrayLoader = new Gs2Bind.Gs2Exchange.RateModelArrayLoader("LimitAdUse");
            _unsubscribers.Add(arrayLoader.Subscribe(
                _gs2,
                _session,
                async (_, _, items) =>
                {
                    if (_disposed) return;
                    try
                    {
                        await ReconcileFromExchangeLimitAdUseMasterItems(items, attachChildSubscribe: true, CancellationToken.None);
                    }
                    catch (Exception ex)
                    {
                        if (onError != null) onError(ex); else UnityEngine.Debug.LogException(ex);
                    }
                },
                notify
            ));
        }

        private async Task ReconcileFromExchangeLimitAdUseMasterItems(IList<Gs2.Unity.Gs2Exchange.Model.EzRateModel> items, bool attachChildSubscribe, CancellationToken cancellationToken)
        {
            var seen = new HashSet<string>();
            foreach (var item in items)
            {
                if (_disposed) return;
                var rowKey = ExtractExchangeLimitAdUseMasterRowKey(item);
                if (rowKey == null) continue;
                seen.Add(rowKey);
                if (_bindersByRowKey.TryGetValue(rowKey, out var existing))
                {
                    ApplyExchangeLimitAdUseMasterItemTo(existing.MutableModel, item);
                }
                else
                {
                    var binder = await BuildBinderFromExchangeLimitAdUseMasterItem(item, cancellationToken);
                    if (_disposed)
                    {
                        binder.Dispose();
                        return;
                    }
                    if (_bindersByRowKey.TryGetValue(rowKey, out var raced))
                    {
                        ApplyExchangeLimitAdUseMasterItemTo(raced.MutableModel, item);
                        binder.Dispose();
                        continue;
                    }
                    if (attachChildSubscribe) binder.Subscribe(_onChange);
                    _bindersByRowKey[rowKey] = binder;
                    _binders.Add(binder);
                    ItemAdded?.Invoke(binder);
                }
            }
            if (_bindersByRowKey.Count > seen.Count)
            {
                var toRemove = new List<string>();
                foreach (var kv in _bindersByRowKey)
                {
                    if (!seen.Contains(kv.Key)) toRemove.Add(kv.Key);
                }
                // Detach every stale binder before callbacks; a callback may dispose this Collection.
                var pendingRemovals = new List<Action>();
                foreach (var rowKey in toRemove)
                {
                    if (!_bindersByRowKey.TryGetValue(rowKey, out var binder)) continue;
                    _bindersByRowKey.Remove(rowKey);
                    _binders.Remove(binder);
                    var detachedBinder = binder;
                    // Notify while the binder is live; the Collection retains disposal ownership.
                    pendingRemovals.Add(() =>
                    {
                        try
                        {
                            if (!_disposed) ItemRemoved?.Invoke(detachedBinder);
                        }
                        finally
                        {
                            detachedBinder.Dispose();
                        }
                    });
                }
                Exception? removalError = null;
                foreach (var remove in pendingRemovals)
                {
                    try { remove(); }
                    catch (Exception ex) { removalError ??= ex; }
                }
                if (removalError != null) throw removalError;
            }
            if (!_disposed) SortBinders();
        }

        private async Task<UsageLimitCounterBinder> BuildBinderFromExchangeLimitAdUseMasterItem(Gs2.Unity.Gs2Exchange.Model.EzRateModel item, CancellationToken cancellationToken)
        {
            var model = UsageLimitCounterBinder.CreateModel((string.IsNullOrEmpty(item.Name) ? default(UsageLimitCounterId) : new UsageLimitCounterId(item.Name)), new UsageLimitId(_limit));
            ApplyExchangeLimitAdUseMasterItemTo(model, item);
            var binder = new UsageLimitCounterBinder(model, _gs2, _session);
            await binder.MountAsync(cancellationToken);
            return binder;
        }

        private static void ApplyExchangeLimitAdUseMasterItemTo(MutableUsageLimitCounter model, Gs2.Unity.Gs2Exchange.Model.EzRateModel item)
        {
            // This loader carries no master-item field assignments; reconcile manages
            // membership only (per-element field changes are tracked by each element
            // binder's own Subscribe).
            _ = item;
            _ = model;
        }

        private UsageLimitCounterId ExtractExchangeLimitAdUseMasterIdentity(Gs2.Unity.Gs2Exchange.Model.EzRateModel item)
        {
            return (string.IsNullOrEmpty(item.Name) ? default(UsageLimitCounterId) : new UsageLimitCounterId(item.Name));
        }

        private string? ExtractExchangeLimitAdUseMasterRowKey(Gs2.Unity.Gs2Exchange.Model.EzRateModel item)
        {
            var id = ExtractExchangeLimitAdUseMasterIdentity(item);
            if (EqualityComparer<UsageLimitCounterId>.Default.Equals(id, default)) return null;
            if (string.IsNullOrEmpty(item.Name)) return null;
            return $"{item.Name}";
        }
        /// <summary>One-shot Create + MountFromLimitLimitUserDataAsync (no subscription).</summary>
        public static async Task<UsageLimitCounterBinderCollection> CreateFromLimitLimitUserDataAsync(
            Gs2Domain gs2,
            IGameSession session,
            string limit,
            CancellationToken cancellationToken = default)
        {
            var coll = new UsageLimitCounterBinderCollection(gs2, session, limit);
            await coll.MountFromLimitLimitUserDataAsync(cancellationToken);
            return coll;
        }

        public async Task MountFromLimitLimitUserDataAsync(CancellationToken cancellationToken = default)
        {
            ThrowIfDisposed();
            cancellationToken.ThrowIfCancellationRequested();
            var arrayLoader = new Gs2Bind.Gs2Limit.CounterArrayLoader("Limit");
            var items = await arrayLoader.Load(_gs2, _session);
            cancellationToken.ThrowIfCancellationRequested();
            await ReconcileFromLimitLimitUserItems(items, attachChildSubscribe: false, cancellationToken);
            _mounted = true;
        }

        public void SubscribeFromLimitLimitUserData(Action? onChange = null, Action<Exception>? onError = null)
        {
            ThrowIfDisposed();
            if (_subscriptionActive) throw new InvalidOperationException("Already subscribed");
            _subscriptionActive = true;
            // Consumer-facing notification, wrapped once so a throwing consumer
            // callback routes to onError (or Debug) instead of escaping the
            // loader's async-void chain.
            Action notify = () =>
            {
                try { onChange?.Invoke(); }
                catch (Exception ex) { if (onError != null) onError(ex); else UnityEngine.Debug.LogException(ex); }
            };
            _onChange = notify;
            foreach (var b in _binders) b.Subscribe(notify);
            var arrayLoader = new Gs2Bind.Gs2Limit.CounterArrayLoader("Limit");
            _unsubscribers.Add(arrayLoader.Subscribe(
                _gs2,
                _session,
                async (_, _, items) =>
                {
                    if (_disposed) return;
                    try
                    {
                        await ReconcileFromLimitLimitUserItems(items, attachChildSubscribe: true, CancellationToken.None);
                    }
                    catch (Exception ex)
                    {
                        if (onError != null) onError(ex); else UnityEngine.Debug.LogException(ex);
                    }
                },
                notify
            ));
        }

        private async Task ReconcileFromLimitLimitUserItems(IList<Gs2.Unity.Gs2Limit.Model.EzCounter> items, bool attachChildSubscribe, CancellationToken cancellationToken)
        {
            var seen = new HashSet<string>();
            foreach (var item in items)
            {
                if (_disposed) return;
                var rowKey = ExtractLimitLimitUserRowKey(item);
                if (rowKey == null) continue;
                seen.Add(rowKey);
                if (_bindersByRowKey.TryGetValue(rowKey, out var existing))
                {
                    ApplyLimitLimitUserItemTo(existing.MutableModel, item);
                }
                else
                {
                    var binder = await BuildBinderFromLimitLimitUserItem(item, cancellationToken);
                    if (_disposed)
                    {
                        binder.Dispose();
                        return;
                    }
                    if (_bindersByRowKey.TryGetValue(rowKey, out var raced))
                    {
                        ApplyLimitLimitUserItemTo(raced.MutableModel, item);
                        binder.Dispose();
                        continue;
                    }
                    if (attachChildSubscribe) binder.Subscribe(_onChange);
                    _bindersByRowKey[rowKey] = binder;
                    _binders.Add(binder);
                    ItemAdded?.Invoke(binder);
                }
            }
            if (_bindersByRowKey.Count > seen.Count)
            {
                var toRemove = new List<string>();
                foreach (var kv in _bindersByRowKey)
                {
                    if (!seen.Contains(kv.Key)) toRemove.Add(kv.Key);
                }
                // Detach every stale binder before callbacks; a callback may dispose this Collection.
                var pendingRemovals = new List<Action>();
                foreach (var rowKey in toRemove)
                {
                    if (!_bindersByRowKey.TryGetValue(rowKey, out var binder)) continue;
                    _bindersByRowKey.Remove(rowKey);
                    _binders.Remove(binder);
                    var detachedBinder = binder;
                    // Notify while the binder is live; the Collection retains disposal ownership.
                    pendingRemovals.Add(() =>
                    {
                        try
                        {
                            if (!_disposed) ItemRemoved?.Invoke(detachedBinder);
                        }
                        finally
                        {
                            detachedBinder.Dispose();
                        }
                    });
                }
                Exception? removalError = null;
                foreach (var remove in pendingRemovals)
                {
                    try { remove(); }
                    catch (Exception ex) { removalError ??= ex; }
                }
                if (removalError != null) throw removalError;
            }
            if (!_disposed) SortBinders();
        }

        private async Task<UsageLimitCounterBinder> BuildBinderFromLimitLimitUserItem(Gs2.Unity.Gs2Limit.Model.EzCounter item, CancellationToken cancellationToken)
        {
            var model = UsageLimitCounterBinder.CreateModel((string.IsNullOrEmpty(item.Name) ? default(UsageLimitCounterId) : new UsageLimitCounterId(item.Name)), new UsageLimitId(_limit));
            ApplyLimitLimitUserItemTo(model, item);
            var binder = new UsageLimitCounterBinder(model, _gs2, _session);
            await binder.MountAsync(cancellationToken);
            return binder;
        }

        private static void ApplyLimitLimitUserItemTo(MutableUsageLimitCounter model, Gs2.Unity.Gs2Limit.Model.EzCounter item)
        {
            _ = item;
            _ = model;
        }

        private UsageLimitCounterId ExtractLimitLimitUserIdentity(Gs2.Unity.Gs2Limit.Model.EzCounter item)
        {
            return (string.IsNullOrEmpty(item.Name) ? default(UsageLimitCounterId) : new UsageLimitCounterId(item.Name));
        }

        private string? ExtractLimitLimitUserRowKey(Gs2.Unity.Gs2Limit.Model.EzCounter item)
        {
            var id = ExtractLimitLimitUserIdentity(item);
            if (EqualityComparer<UsageLimitCounterId>.Default.Equals(id, default)) return null;
            if (string.IsNullOrEmpty(item.LimitName) || string.IsNullOrEmpty(item.Name)) return null;
            return $"{item.LimitName}.{item.Name}";
        }

        internal sealed class UsageLimitCounterBinderComparer : IComparer<IReadOnlyUsageLimitCounterBinder>
        {
            public static readonly UsageLimitCounterBinderComparer Default = new UsageLimitCounterBinderComparer();
            public int Compare(IReadOnlyUsageLimitCounterBinder? a, IReadOnlyUsageLimitCounterBinder? b)
            {
                if (a is null && b is null) return 0;
                if (a is null) return -1;
                if (b is null) return 1;
                return Comparer<UsageLimitCounterId>.Default.Compare(a.Id, b.Id);
            }
        }

        internal sealed class UsageLimitCounterBinderIdTieBreakComparer : IComparer<IReadOnlyUsageLimitCounterBinder>
        {
            private readonly IComparer<IReadOnlyUsageLimitCounterBinder> _inner;
            public UsageLimitCounterBinderIdTieBreakComparer(IComparer<IReadOnlyUsageLimitCounterBinder> inner) { _inner = inner; }
            public int Compare(IReadOnlyUsageLimitCounterBinder? a, IReadOnlyUsageLimitCounterBinder? b)
            {
                if (a is null && b is null) return 0;
                if (a is null) return -1;
                if (b is null) return 1;
                var c = _inner.Compare(a, b);
                if (c != 0) return c;
                return Comparer<UsageLimitCounterId>.Default.Compare(a.Id, b.Id);
            }
        }
    }
}
